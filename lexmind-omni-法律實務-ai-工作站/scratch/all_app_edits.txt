=== Line 235 | Step 125 | Tool replace_file_content ===
Description: Add currentFileElapsedSeconds state to track time spent on the active file in App.tsx
Instruction: Add currentFileElapsedSeconds state below batchElapsedSeconds.
TargetContent:
  const [batchElapsedSeconds, setBatchElapsedSeconds] = useState(0);

ReplacementContent:
  const [batchElapsedSeconds, setBatchElapsedSeconds] = useState(0);
  const [currentFileElapsedSeconds, setCurrentFileElapsedSeconds] = useState(0);

--------------------------------------------------------------------------------

=== Line 237 | Step 127 | Tool replace_file_content ===
Description: Update runBatchIngestProcess in App.tsx to track file progress timer, set batchCurrentFileName, reset timers per file, handle non-JSON HTTP errors gracefully, and clear states upon completion/abort.
Instruction: Update runBatchIngestProcess function to set current active file states, reset currentFileElapsedSeconds, handle response parse failures gracefully, and clear states on exit.
TargetContent:
  const runBatchIngestProcess = async (
    filesToProcess: typeof scannedLocalFiles,
    targetMode: "import" | "direct",
    execMode: "all" | "batch",
    filesPerBatch: number,
    intervalSec: number
  ) => {
    isBatchPausedRef.current = false;
    isBatchAbortedRef.current = false;
    setBatchIngestStatus("running");
    setBatchProcessedCount(0);
    setBatchElapsedSeconds(0);
    
    const startTimeStr = new Date().toLocaleTimeString();
    setBatchLogs([
      { time: startTimeStr, type: "info", text: `🚀 開始處理 ${filesToProcess.length} 個本機教材檔案...` },
      { time: startTimeStr, type: "info", text: `ℹ️ 模式: ${targetMode === "import" ? "導入待處理隊列" : "直接分析歸類到記憶庫"}` }
    ]);
    
    // Set up timer
    let timerId = setInterval(() => {
      if (!isBatchPausedRef.current) {
        setBatchElapsedSeconds(prev => prev + 1);
      }
    }, 1000);
    
    const stats = {
      successCount: 0,
      failCount: 0,
      processedFiles: [] as typeof scannedLocalFiles
    };

    const processSingleFile = async (
      fileInfo: typeof scannedLocalFiles[0],
      targetMode: "import" | "direct"
    ) => {
      const prependedName = fileInfo.name;
      try {
        // Check pause
        if (isBatchPausedRef.current) {
          setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "info", text: `⏸️ 檔案 ${prependedName} 暫停等待中...` }]);
          while (isBatchPausedRef.current) {
            if (isBatchAbortedRef.current) break;
            await new Promise(r => setTimeout(r, 200));
          }
          if (isBatchAbortedRef.current) return;
        }

        // Call local GPU-accelerated file parser directly on the server
        const fileExt = fileInfo.ext || fileInfo.name.split('.').pop() || "";
        const parseResp = await fetch("/api/parse-local-file", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ path: fileInfo.path, ext: fileExt })
        });
        
        if (!parseResp.ok) {
          const errData = await parseResp.json();
          throw new Error(errData.error || `本地/GPU 檔案解析失敗 (HTTP ${parseResp.status})`);
        }
        const parseData = await parseResp.json();
        const content = parseData.content || "";
        
        if (targetMode === "import") {
          const newFile: BatchFile = {
            name: prependedName,
            content: content,
            category: classifyFileCategory(prependedName)
          };
          setBatchTexts(prev => {
            if (prev.some(b => b.name === newFile.name)) return prev;
            return [newFile, ...prev];
          });
          setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "success", text: `✅ [完成] 檔案 ${prependedName} 成功導入待處理隊列！` }]);
        } else {
          // Direct Ingest
          const ingestResp = await fetch("/api/ingest", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              files: [{
                name: prependedName,
                content: content,
                category: classifyFileCategory(prependedName)
              }]
            })
          });
          if (!ingestResp.ok) {
            throw new Error(`直接歸類分析 API 失敗 (HTTP ${ingestResp.status})`);
          }
          const data = await ingestResp.json();
          if (data.status === "success") {
            if (data.totalVaultItems) {
              setTotalVaultItems(data.totalVaultItems);
            }
            setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "success", text: `✅ [完成] 檔案 ${prependedName} 成功分析並歸類到知識庫！` }]);
          } else {
            throw new Error(data.error || "內部歸類錯誤");
          }
        }
        
        stats.successCount++;
        stats.processedFiles.push(fileInfo);
      } catch (fileErr: any) {
        stats.failCount++;
        const errMsg = fileErr.message || String(fileErr);
        setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "error", text: `❌ 檔案 ${prependedName} 處理失敗: ${errMsg}` }]);
        
        // Log error to backend logger
        try {
          await fetch("/api/log-error", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              filename: prependedName,
              error: errMsg,
              timestamp: new Date().toISOString(),
              type: targetMode
            })
          });
        } catch (logErr) {
          console.error("Failed to post error log to server:", logErr);
        }
      }
    };

    try {
      if (execMode === "all") {
        const concurrency = 2;
        let currentIndex = 0;
        
        const runWorker = async () => {
          while (currentIndex < filesToProcess.length) {
            if (isBatchAbortedRef.current) break;
            const index = currentIndex++;
            if (index >= filesToProcess.length) break;
            const fileInfo = filesToProcess[index];
            const prependedName = fileInfo.name;
            
            // Log starting
            setBatchLogs(prev => [...prev, { 
              time: new Date().toLocaleTimeString(), 
              type: "info", 
              text: `🔄 [啟動] 正在分析 (${index + 1}/${filesToProcess.length}): ${prependedName} (${((fileInfo.size || 0) / (1024*1024)).toFixed(2)} MB)...` 
            }]);
            
            await processSingleFile(fileInfo, targetMode);
            setBatchProcessedCount(prev => Math.min(filesToProcess.length, prev + 1));
          }
        };
        
        const workers = [];
        const numWorkers = Math.min(concurrency, filesToProcess.length);
        for (let i = 0; i < numWorkers; i++) {
          workers.push(runWorker());
        }
        await Promise.all(workers);
      } else {
        // Batch Mode: process files in groups of filesPerBatch
        const concurrency = Math.min(2, filesPerBatch);
        for (let index = 0; index < filesToProcess.length; index += filesPerBatch) {
          if (isBatchAbortedRef.current) break;
          const batchFiles = filesToProcess.slice(index, index + filesPerBatch);
          
          let batchIndex = 0;
          const runWorker = async () => {
            while (batchIndex < batchFiles.length) {
              if (isBatchAbortedRef.current) break;
              const bIndex = batchIndex++;
              if (bIndex >= batchFiles.length) break;
              const fileInfo = batchFiles[bIndex];
              const globalIndex = index + bIndex;
              const prependedName = fileInfo.name;
              
              setBatchLogs(prev => [...prev, { 
                time: new Date().toLocaleTimeString(), 
                type: "info", 
                text: `🔄 [啟動] 正在分析 (${globalIndex + 1}/${filesToProcess.length}): ${prependedName} (${((fileInfo.size || 0) / (1024*1024)).toFixed(2)} MB)...` 
              }]);
              
              await processSingleFile(fileInfo, targetMode);
              setBatchProcessedCount(prev => Math.min(filesToProcess.length, prev + 1));
            }
          };
          
          const workers = [];
          const numWorkers = Math.min(concurrency, batchFiles.length);
          for (let i = 0; i < numWorkers; i++) {
            workers.push(runWorker());
          }
          await Promise.all(workers);
          
          // Wait for interval
          if (index + filesPerBatch < filesToProcess.length && !isBatchAbortedRef.current) {
            setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "info", text: `⏳ 達到批次上限，等待間隔時間 ${intervalSec} 秒...` }]);
            for (let s = 0; s < intervalSec; s++) {
              if (isBatchAbortedRef.current) break;
              await new Promise(r => setTimeout(r, 1000));
            }
          }
        }
      }
      
      clearInterval(timerId);
      
      if (!isBatchAbortedRef.current) {
        setBatchIngestStatus("completed");
        setBatchLogs(prev => [...prev, {
          time: new Date().toLocaleTimeString(),
          type: "success",
          text: `🎉 處理完成！成功：${stats.successCount} 個，失敗：${stats.failCount} 個。`
        }]);
        showAlert(`✅ 本機教材批次處理完成！成功 ${stats.successCount} 個，失敗 ${stats.failCount} 個！`);
        
        // Remove processed files from UI scanned list
        setScannedLocalFiles(prev => prev.filter(f => !stats.processedFiles.some(p => p.path === f.path)));
        
        if (targetMode === "direct") {
          fetchSystemStatus(); // Refresh DB stats
        }
      } else {
        setBatchIngestStatus("aborted");
        setBatchLogs(prev => [...prev, {
          time: new Date().toLocaleTimeString(),
          type: "error",
          text: `🛑 處理程序已被使用者手動中斷。`
        }]);
      }
    } catch (err: any) {
      clearInterval(timerId);
      setBatchIngestStatus("aborted");
      const errMsg = err?.message || String(err);
      if (errMsg === "aborted") {
        setBatchLogs(prev => [
          ...prev,
          { time: new Date().toLocaleTimeString(), type: "error", text: "🛑 處理程序已被使用者手動中斷。" }
        ]);
        showAlert("🛑 批次處理程序已被使用者手動中斷與取消。");
      } else {
        setBatchLogs(prev => [
          ...prev, 
          { time: new Date().toLocaleTimeString(), type: "error", text: `❌ 偵測到執行異常 (Bug): ${errMsg}` },
          { time: new Date().toLocaleTimeString(), type: "info", text: "⚠️ [系統提示] 已將 Bug 細節傳回後端檢測。請等候後續軟體版本改版與修復，避免直接使用。" }
        ]);
        showAlert(`⚠️ [系統檢測回報]\n偵測到程式碼執行 Bug：\n${errMsg}\n\n已將錯誤細節送回後端檢測，請等候下一軟體版本改版。視窗即將解鎖，您可以點選「關閉視窗」安全退出。`);
        
        try {
          fetch("/api/log-error", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              filename: "BATCH_PROCESS_FATAL",
              error: errMsg,
              timestamp: new Date().toISOString(),
              type: "FATAL_BUG"
            })
          });
        } catch (logErr) {
          console.error("Failed to post error log to server:", logErr);
        }
      }
    }

ReplacementContent:
  const runBatchIngestProcess = async (
    filesToProcess: typeof scannedLocalFiles,
    targetMode: "import" | "direct",
    execMode: "all" | "batch",
    filesPerBatch: number,
    intervalSec: number
  ) => {
    isBatchPausedRef.current = false;
    isBatchAbortedRef.current = false;
    setBatchIngestStatus("running");
    setBatchProcessedCount(0);
    setBatchElapsedSeconds(0);
    setCurrentFileElapsedSeconds(0);
    
    const startTimeStr = new Date().toLocaleTimeString();
    setBatchLogs([
      { time: startTimeStr, type: "info", text: `🚀 開始處理 ${filesToProcess.length} 個本機教材檔案...` },
      { time: startTimeStr, type: "info", text: `ℹ️ 模式: ${targetMode === "import" ? "導入待處理隊列" : "直接分析歸類到記憶庫"}` }
    ]);
    
    // Set up timer
    let timerId = setInterval(() => {
      if (!isBatchPausedRef.current) {
        setBatchElapsedSeconds(prev => prev + 1);
        setCurrentFileElapsedSeconds(prev => prev + 1);
      }
    }, 1000);
    
    const stats = {
      successCount: 0,
      failCount: 0,
      processedFiles: [] as typeof scannedLocalFiles
    };

    const processSingleFile = async (
      fileInfo: typeof scannedLocalFiles[0],
      targetMode: "import" | "direct"
    ) => {
      const prependedName = fileInfo.name;
      try {
        // Check pause
        if (isBatchPausedRef.current) {
          setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "info", text: `⏸️ 檔案 ${prependedName} 暫停等待中...` }]);
          while (isBatchPausedRef.current) {
            if (isBatchAbortedRef.current) break;
            await new Promise(r => setTimeout(r, 200));
          }
          if (isBatchAbortedRef.current) return;
        }

        // Call local GPU-accelerated file parser directly on the server
        const fileExt = fileInfo.ext || fileInfo.name.split('.').pop() || "";
        const parseResp = await fetch("/api/parse-local-file", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ path: fileInfo.path, ext: fileExt })
        });
        
        if (!parseResp.ok) {
          let errorMsg = `本地/GPU 檔案解析失敗 (HTTP ${parseResp.status})`;
          try {
            const errData = await parseResp.json();
            errorMsg = errData.error || errorMsg;
          } catch (e) {
            try {
              const text = await parseResp.text();
              if (text) errorMsg = text.slice(0, 150);
            } catch {}
          }
          throw new Error(errorMsg);
        }
        const parseData = await parseResp.json();
        const content = parseData.content || "";
        
        if (targetMode === "import") {
          const newFile: BatchFile = {
            name: prependedName,
            content: content,
            category: classifyFileCategory(prependedName)
          };
          setBatchTexts(prev => {
            if (prev.some(b => b.name === newFile.name)) return prev;
            return [newFile, ...prev];
          });
          setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "success", text: `✅ [完成] 檔案 ${prependedName} 成功導入待處理隊列！` }]);
        } else {
          // Direct Ingest
          const ingestResp = await fetch("/api/ingest", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              files: [{
                name: prependedName,
                content: content,
                category: classifyFileCategory(prependedName)
              }]
            })
          });
          if (!ingestResp.ok) {
            throw new Error(`直接歸類分析 API 失敗 (HTTP ${ingestResp.status})`);
          }
          const data = await ingestResp.json();
          if (data.status === "success") {
            if (data.totalVaultItems) {
              setTotalVaultItems(data.totalVaultItems);
            }
            setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "success", text: `✅ [完成] 檔案 ${prependedName} 成功分析並歸類到知識庫！` }]);
          } else {
            throw new Error(data.error || "內部歸類錯誤");
          }
        }
        
        stats.successCount++;
        stats.processedFiles.push(fileInfo);
      } catch (fileErr: any) {
        stats.failCount++;
        const errMsg = fileErr.message || String(fileErr);
        setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "error", text: `❌ 檔案 ${prependedName} 處理失敗: ${errMsg}` }]);
        
        // Log error to backend logger
        try {
          await fetch("/api/log-error", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              filename: prependedName,
              error: errMsg,
              timestamp: new Date().toISOString(),
              type: targetMode
            })
          });
        } catch (logErr) {
          console.error("Failed to post error log to server:", logErr);
        }
      }
    };

    try {
      if (execMode === "all") {
        const concurrency = 2;
        let currentIndex = 0;
        
        const runWorker = async () => {
          while (currentIndex < filesToProcess.length) {
            if (isBatchAbortedRef.current) break;
            const index = currentIndex++;
            if (index >= filesToProcess.length) break;
            const fileInfo = filesToProcess[index];
            const prependedName = fileInfo.name;
            
            // Log starting
            setBatchLogs(prev => [...prev, { 
              time: new Date().toLocaleTimeString(), 
              type: "info", 
              text: `🔄 [啟動] 正在分析 (${index + 1}/${filesToProcess.length}): ${prependedName} (${((fileInfo.size || 0) / (1024*1024)).toFixed(2)} MB)...` 
            }]);
            
            setBatchCurrentFileName(prependedName);
            setCurrentFileElapsedSeconds(0);
            await processSingleFile(fileInfo, targetMode);
            setBatchProcessedCount(prev => Math.min(filesToProcess.length, prev + 1));
          }
        };
        
        const workers = [];
        const numWorkers = Math.min(concurrency, filesToProcess.length);
        for (let i = 0; i < numWorkers; i++) {
          workers.push(runWorker());
        }
        await Promise.all(workers);
      } else {
        // Batch Mode: process files in groups of filesPerBatch
        const concurrency = Math.min(2, filesPerBatch);
        for (let index = 0; index < filesToProcess.length; index += filesPerBatch) {
          if (isBatchAbortedRef.current) break;
          const batchFiles = filesToProcess.slice(index, index + filesPerBatch);
          
          let batchIndex = 0;
          const runWorker = async () => {
            while (batchIndex < batchFiles.length) {
              if (isBatchAbortedRef.current) break;
              const bIndex = batchIndex++;
              if (bIndex >= batchFiles.length) break;
              const fileInfo = batchFiles[bIndex];
              const globalIndex = index + bIndex;
              const prependedName = fileInfo.name;
              
              setBatchLogs(prev => [...prev, { 
                time: new Date().toLocaleTimeString(), 
                type: "info", 
                text: `🔄 [啟動] 正在分析 (${globalIndex + 1}/${filesToProcess.length}): ${prependedName} (${((fileInfo.size || 0) / (1024*1024)).toFixed(2)} MB)...` 
              }]);
              
              setBatchCurrentFileName(prependedName);
              setCurrentFileElapsedSeconds(0);
              await processSingleFile(fileInfo, targetMode);
              setBatchProcessedCount(prev => Math.min(filesToProcess.length, prev + 1));
            }
          };
          
          const workers = [];
          const numWorkers = Math.min(concurrency, batchFiles.length);
          for (let i = 0; i < numWorkers; i++) {
            workers.push(runWorker());
          }
          await Promise.all(workers);
          
          // Wait for interval
          if (index + filesPerBatch < filesToProcess.length && !isBatchAbortedRef.current) {
            setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "info", text: `⏳ 達到批次上限，等待間隔時間 ${intervalSec} 秒...` }]);
            for (let s = 0; s < intervalSec; s++) {
              if (isBatchAbortedRef.current) break;
              await new Promise(r => setTimeout(r, 1000));
            }
          }
        }
      }
      
      clearInterval(timerId);
      setBatchCurrentFileName("");
      setCurrentFileElapsedSeconds(0);
      
      if (!isBatchAbortedRef.current) {
        setBatchIngestStatus("completed");
        setBatchLogs(prev => [...prev, {
          time: new Date().toLocaleTimeString(),
          type: "success",
          text: `🎉 處理完成！成功：${stats.successCount} 個，失敗：${stats.failCount} 個。`
        }]);
        showAlert(`✅ 本機教材批次處理完成！成功 ${stats.successCount} 個，失敗 ${stats.failCount} 個！`);
        
        // Remove processed files from UI scanned list
        setScannedLocalFiles(prev => prev.filter(f => !stats.processedFiles.some(p => p.path === f.path)));
        
        if (targetMode === "direct") {
          fetchSystemStatus(); // Refresh DB stats
        }
      } else {
        setBatchIngestStatus("aborted");
        setBatchLogs(prev => [...prev, {
          time: new Date().toLocaleTimeString(),
          type: "error",
          text: `🛑 處理程序已被使用者手動中斷。`
        }]);
      }
    } catch (err: any) {
      clearInterval(timerId);
      setBatchCurrentFileName("");
      setCurrentFileElapsedSeconds(0);
      setBatchIngestStatus("aborted");
      const errMsg = err?.message || String(err);
      if (errMsg === "aborted") {
        setBatchLogs(prev => [
          ...prev,
          { time: new Date().toLocaleTimeString(), type: "error", text: "🛑 處理程序已被使用者手動中斷。" }
        ]);
        showAlert("🛑 批次處理程序已被使用者手動中斷與取消。");
      } else {
        setBatchLogs(prev => [
          ...prev, 
          { time: new Date().toLocaleTimeString(), type: "error", text: `❌ 偵測到執行異常 (Bug): ${errMsg}` },
          { time: new Date().toLocaleTimeString(), type: "info", text: "⚠️ [系統提示] 已將 Bug 細節傳回後端檢測。請等候後續軟體版本改版與修復，避免直接使用。" }
        ]);
        showAlert(`⚠️ [系統檢測回報]\n偵測到程式碼執行 Bug：\n${errMsg}\n\n已將錯誤細節送回後端檢測，請等候下一軟體版本改版。視窗即將解鎖，您可以點選「關閉視窗」安全退出。`);
        
        try {
          fetch("/api/log-error", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              filename: "BATCH_PROCESS_FATAL",
              error: errMsg,
              timestamp: new Date().toISOString(),
              type: "FATAL_BUG"
            })
          });
        } catch (logErr) {
          console.error("Failed to post error log to server:", logErr);
        }
      }
    }

--------------------------------------------------------------------------------

=== Line 243 | Step 133 | Tool replace_file_content ===
Description: Render a warning box if a single file has been processing for more than 10 minutes (600 seconds) in App.tsx
Instruction: Replace the batchIngestStatus === "running" details block to show a warning if the file takes more than 10 minutes.
TargetContent:
                    {batchIngestStatus === "running" && (
                      <div className="flex items-center gap-2 text-slate-300">
                        <RefreshCw className="w-3.5 h-3.5 animate-spin text-amber-500" />
                        <span className="truncate">
                          正在處理：<span className="font-mono font-bold text-white">{batchCurrentFileName}</span>
                        </span>
                      </div>
                    )}

ReplacementContent:
                    {batchIngestStatus === "running" && (
                      <div className="flex flex-col gap-2">
                        <div className="flex items-center gap-2 text-slate-300">
                          <RefreshCw className="w-3.5 h-3.5 animate-spin text-amber-500" />
                          <span className="truncate">
                            正在處理：<span className="font-mono font-bold text-white">{batchCurrentFileName}</span>
                          </span>
                        </div>
                        {currentFileElapsedSeconds >= 600 && (
                          <div className="p-2 bg-amber-950/30 border border-amber-900/50 rounded text-amber-500 text-[10px] leading-relaxed mt-1 animate-pulse">
                            ⚠️ 此檔案已持續轉錄處理超過 {Math.floor(currentFileElapsedSeconds / 60)} 分鐘。超大影音（數 GB）由於 GPU 進行 Whisper 實時轉譯及大模型校正耗能極大，通常需 10-30 分鐘。系統正常運作中，請勿關閉視窗。
                          </div>
                        )}
                      </div>
                    )}

--------------------------------------------------------------------------------

=== Line 618 | Step 505 | Tool replace_file_content ===
Description: Declare state variable scanFilterQuery to allow real-time filtering of crawled files.
Instruction: Add const [scanFilterQuery, setScanFilterQuery] = useState(""); right below the scannedLocalFiles state hook.
TargetContent:
  const [scannedLocalFiles, setScannedLocalFiles] = useState<{ name: string; path: string; size: number; ext: string }[]>([]);
  const [isLocalImporting, setIsLocalImporting] = useState(false);

ReplacementContent:
  const [scannedLocalFiles, setScannedLocalFiles] = useState<{ name: string; path: string; size: number; ext: string }[]>([]);
  const [scanFilterQuery, setScanFilterQuery] = useState("");
  const [isLocalImporting, setIsLocalImporting] = useState(false);

--------------------------------------------------------------------------------

=== Line 622 | Step 509 | Tool replace_file_content ===
Description: Add client-side filtering query and text input to filter scanned files and dynamically adjust batch ingestion targets.
Instruction: Replace the scanned files results rendering block with the new filtered rendering block containing the scanFilterQuery input bar.
TargetContent:
                    {/* Scanned files results display */}
                    {scannedLocalFiles.length > 0 ? (
                      <div className="flex flex-col gap-2 mt-1 bg-slate-950 border border-slate-850 rounded-xl p-3 max-h-[200px] overflow-y-auto">
                        <div className="text-[10px] font-bold text-slate-500 uppercase tracking-wider flex justify-between">
                          <span>🆕 尚未分析之教材 ({scannedLocalFiles.length} 個檔案)</span>
                          <span>操作</span>
                        </div>
                        <div className="space-y-1.5 mt-1">
                          {scannedLocalFiles.map((file, i) => (
                            <div key={i} className="flex justify-between items-center text-[11px] py-1 border-b border-slate-850/40 last:border-0 pb-1">
                              <span className="text-slate-300 truncate max-w-[240px] flex items-center gap-1.5 font-mono" title={file.path}>
                                <span className="px-1 py-0.5 bg-slate-900 border border-slate-800 text-[8px] font-bold text-slate-500 rounded uppercase">
                                  {file.ext}
                                </span>
                                {file.name}
                                <span className="text-[9px] text-slate-550 font-sans">
                                  ({(file.size / (1024 * 1024)).toFixed(2)} MB)
                                </span>
                              </span>
                              <div className="flex gap-1.5">
                                <button
                                  type="button"
                                  disabled={isLocalImporting}
                                  onClick={() => handleImportLocalFiles([file])}
                                  className="px-1.5 py-0.5 bg-slate-900 hover:bg-slate-800 border border-slate-800 text-slate-300 rounded text-[9px] cursor-pointer transition-colors"
                                >
                                  📥 放入餵食區
                                </button>
                                <button
                                  type="button"
                                  disabled={isLocalImporting}
                                  onClick={() => handleDirectIngestLocalFiles([file])}
                                  className="px-1.5 py-0.5 bg-amber-500/10 hover:bg-amber-500/20 border border-amber-500/25 text-amber-400 rounded text-[9px] cursor-pointer transition-colors"
                                >
                                  ⚡ 直接分析歸類
                                </button>
                              </div>
                            </div>
                          ))}
                        </div>

                        {/* Batch Action Buttons */}
                        <div className="flex gap-2 mt-2 pt-2 border-t border-slate-850">
                          <button
                            type="button"
                            disabled={isLocalImporting}
                            onClick={() => handleImportLocalFiles(scannedLocalFiles)}
                            className="flex-1 py-1.5 bg-slate-900 border border-slate-800 hover:bg-slate-850 text-slate-300 font-semibold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                          >
                            📥 全數放入餵食區
                          </button>
                          <button
                            type="button"
                            disabled={isLocalImporting}
                            onClick={() => handleDirectIngestLocalFiles(scannedLocalFiles)}
                            className="flex-1 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 font-bold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                          >
                            ⚡ 全數一鍵分析並歸類到記憶庫
                          </button>
                        </div>
                      </div>
                    ) : (

ReplacementContent:
                    {/* Scanned files results display */}
                    {scannedLocalFiles.length > 0 ? (() => {
                      const filteredFiles = scannedLocalFiles.filter(file => {
                        const q = scanFilterQuery.toLowerCase().trim();
                        if (!q) return true;
                        return file.name.toLowerCase().includes(q) || file.path.toLowerCase().includes(q);
                      });
                      return (
                        <div className="flex flex-col gap-2 mt-1 bg-slate-950 border border-slate-850 rounded-xl p-3 max-h-[250px] overflow-y-auto">
                          
                          {/* Search Filter Input Box */}
                          <div className="flex gap-2 mb-1 border-b border-slate-850/40 pb-2">
                            <input
                              type="text"
                              value={scanFilterQuery}
                              onChange={(e) => setScanFilterQuery(e.target.value)}
                              placeholder="🔍 輸入關鍵字篩選教材名（如：民法、證物）..."
                              className="flex-1 bg-slate-900 border border-slate-800 focus:border-amber-500/50 rounded-md py-1 px-2.5 text-[10px] text-slate-350 focus:outline-none transition-all font-sans"
                            />
                            {scanFilterQuery && (
                              <button
                                type="button"
                                onClick={() => setScanFilterQuery("")}
                                className="px-2 py-1 bg-slate-900 hover:bg-slate-850 border border-slate-800 text-slate-400 rounded text-[9px] cursor-pointer transition-colors"
                              >
                                清除
                              </button>
                            )}
                          </div>

                          <div className="text-[10px] font-bold text-slate-500 uppercase tracking-wider flex justify-between">
                            <span>🆕 尚未分析之教材 ({filteredFiles.length} / {scannedLocalFiles.length} 個檔案)</span>
                            <span>操作</span>
                          </div>
                          <div className="space-y-1.5 mt-1">
                            {filteredFiles.map((file, i) => (
                              <div key={i} className="flex justify-between items-center text-[11px] py-1 border-b border-slate-850/40 last:border-0 pb-1">
                                <span className="text-slate-300 truncate max-w-[240px] flex items-center gap-1.5 font-mono" title={file.path}>
                                  <span className="px-1 py-0.5 bg-slate-900 border border-slate-800 text-[8px] font-bold text-slate-500 rounded uppercase">
                                    {file.ext}
                                  </span>
                                  {file.name}
                                  <span className="text-[9px] text-slate-550 font-sans">
                                    ({(file.size / (1024 * 1024)).toFixed(2)} MB)
                                  </span>
                                </span>
                                <div className="flex gap-1.5">
                                  <button
                                    type="button"
                                    disabled={isLocalImporting}
                                    onClick={() => handleImportLocalFiles([file])}
                                    className="px-1.5 py-0.5 bg-slate-900 hover:bg-slate-800 border border-slate-800 text-slate-300 rounded text-[9px] cursor-pointer transition-colors"
                                  >
                                    📥 放入餵食區
                                  </button>
                                  <button
                                    type="button"
                                    disabled={isLocalImporting}
                                    onClick={() => handleDirectIngestLocalFiles([file])}
                                    className="px-1.5 py-0.5 bg-amber-500/10 hover:bg-amber-500/20 border border-amber-500/25 text-amber-400 rounded text-[9px] cursor-pointer transition-colors"
                                  >
                                    ⚡ 直接分析歸類
                                  </button>
                                </div>
                              </div>
                            ))}
                          </div>

                          {/* Batch Action Buttons */}
                          <div className="flex gap-2 mt-2 pt-2 border-t border-slate-850">
                            <button
                              type="button"
                              disabled={isLocalImporting || filteredFiles.length === 0}
                              onClick={() => handleImportLocalFiles(filteredFiles)}
                              className="flex-1 py-1.5 bg-slate-900 border border-slate-800 hover:bg-slate-850 text-slate-300 font-semibold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                            >
                              📥 導入已篩選檔案 ({filteredFiles.length})
                            </button>
                            <button
                              type="button"
                              disabled={isLocalImporting || filteredFiles.length === 0}
                              onClick={() => handleDirectIngestLocalFiles(filteredFiles)}
                              className="flex-1 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 font-bold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                            >
                              ⚡ 分析已篩選檔案 ({filteredFiles.length})
                            </button>
                          </div>
                        </div>
                      );
                    })()) : (

--------------------------------------------------------------------------------

=== Line 630 | Step 517 | Tool replace_file_content ===
Description: Fix typo in IIFE syntax at line 3138 of src/App.tsx.
Instruction: Change })()) : ( to })() : ( at line 3138 in src/App.tsx.
TargetContent:
                    })()) : (

ReplacementContent:
                    })() : (

--------------------------------------------------------------------------------

=== Line 925 | Step 819 | Tool replace_file_content ===
Description: Add status, error, size, path, and elapsedSeconds optional properties to BatchFile interface in App.tsx.
Instruction: Extend the BatchFile interface with background status tracking properties.
TargetContent:
interface BatchFile {
  name: string;
  content: string;
  category: "判例" | "證物" | "學術錄影" | "學術錄音" | "學術教材";
}

ReplacementContent:
interface BatchFile {
  name: string;
  content: string;
  category: "判例" | "證物" | "學術錄影" | "學術錄音" | "學術教材";
  status?: "pending" | "parsing" | "ready" | "error" | "ingested";
  error?: string;
  size?: number;
  path?: string;
  elapsedSeconds?: number;
}

--------------------------------------------------------------------------------

=== Line 929 | Step 823 | Tool replace_file_content ===
Description: Initialize pending file placeholders in batchTexts queue inside runBatchIngestProcess.
Instruction: Add placeholder initialization for targetMode === "import" to App.tsx.
TargetContent:
    isBatchPausedRef.current = false;
    isBatchAbortedRef.current = false;
    setBatchIngestStatus("running");
    setBatchProcessedCount(0);
    setBatchElapsedSeconds(0);
    setCurrentFileElapsedSeconds(0);

ReplacementContent:
    isBatchPausedRef.current = false;
    isBatchAbortedRef.current = false;
    setBatchIngestStatus("running");
    setBatchProcessedCount(0);
    setBatchElapsedSeconds(0);
    setCurrentFileElapsedSeconds(0);
    
    if (targetMode === "import") {
      const placeholders: BatchFile[] = filesToProcess.map(f => ({
        name: f.name,
        content: "",
        category: classifyFileCategory(f.name),
        status: "pending",
        size: f.size,
        path: f.path
      }));
      setBatchTexts(prev => {
        const filteredPrev = prev.filter(p => !placeholders.some(pl => pl.name === p.name));
        return [...filteredPrev, ...placeholders];
      });
    }

--------------------------------------------------------------------------------

=== Line 933 | Step 827 | Tool replace_file_content ===
Description: Update status of files in batchTexts queue dynamically as they process in processSingleFile.
Instruction: Set item status to 'parsing', 'ready', or 'error' during background file processing.
TargetContent:
        // Call local GPU-accelerated file parser directly on the server
        const fileExt = fileInfo.ext || fileInfo.name.split('.').pop() || "";
        const parseResp = await fetch("/api/parse-local-file", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ path: fileInfo.path, ext: fileExt })
        });
        
        if (!parseResp.ok) {
          let errorMsg = `本地/GPU 檔案解析失敗 (HTTP ${parseResp.status})`;
          try {
            const errData = await parseResp.json();
            errorMsg = errData.error || errorMsg;
          } catch (e) {
            try {
              const text = await parseResp.text();
              if (text) errorMsg = text.slice(0, 150);
            } catch {}
          }
          throw new Error(errorMsg);
        }
        const parseData = await parseResp.json();
        const content = parseData.content || "";
        
        if (targetMode === "import") {
          const newFile: BatchFile = {
            name: prependedName,
            content: content,
            category: classifyFileCategory(prependedName)
          };
          setBatchTexts(prev => {
            if (prev.some(b => b.name === newFile.name)) return prev;
            return [newFile, ...prev];
          });
          setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "success", text: `✅ [完成] 檔案 ${prependedName} 成功導入待處理隊列！` }]);
        } else {
          // Direct Ingest
          const ingestResp = await fetch("/api/ingest", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              files: [{
                name: prependedName,
                content: content,
                category: classifyFileCategory(prependedName)
              }]
            })
          });
          if (!ingestResp.ok) {
            throw new Error(`直接歸類分析 API 失敗 (HTTP ${ingestResp.status})`);
          }
          const data = await ingestResp.json();
          if (data.status === "success") {
            if (data.totalVaultItems) {
              setTotalVaultItems(data.totalVaultItems);
            }
            setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "success", text: `✅ [完成] 檔案 ${prependedName} 成功分析並歸類到知識庫！` }]);
          } else {
            throw new Error(data.error || "內部歸類錯誤");
          }
        }
        
        stats.successCount++;
        stats.processedFiles.push(fileInfo);
      } catch (fileErr: any) {
        stats.failCount++;
        const errMsg = fileErr.message || String(fileErr);
        setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "error", text: `❌ 檔案 ${prependedName} 處理失敗: ${errMsg}` }]);

ReplacementContent:
        // Call local GPU-accelerated file parser directly on the server
        if (targetMode === "import") {
          setBatchTexts(prev => prev.map(item => item.name === prependedName ? { ...item, status: "parsing" } : item));
        }

        const fileExt = fileInfo.ext || fileInfo.name.split('.').pop() || "";
        const parseResp = await fetch("/api/parse-local-file", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ path: fileInfo.path, ext: fileExt })
        });
        
        if (!parseResp.ok) {
          let errorMsg = `本地/GPU 檔案解析失敗 (HTTP ${parseResp.status})`;
          try {
            const errData = await parseResp.json();
            errorMsg = errData.error || errorMsg;
          } catch (e) {
            try {
              const text = await parseResp.text();
              if (text) errorMsg = text.slice(0, 150);
            } catch {}
          }
          throw new Error(errorMsg);
        }
        const parseData = await parseResp.json();
        const content = parseData.content || "";
        
        if (targetMode === "import") {
          setBatchTexts(prev => prev.map(item => item.name === prependedName ? { 
            ...item, 
            status: "ready", 
            content: content 
          } : item));
          setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "success", text: `✅ [完成] 檔案 ${prependedName} 成功導入待處理隊列！` }]);
        } else {
          // Direct Ingest
          const ingestResp = await fetch("/api/ingest", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              files: [{
                name: prependedName,
                content: content,
                category: classifyFileCategory(prependedName)
              }]
            })
          });
          if (!ingestResp.ok) {
            throw new Error(`直接歸類分析 API 失敗 (HTTP ${ingestResp.status})`);
          }
          const data = await ingestResp.json();
          if (data.status === "success") {
            if (data.totalVaultItems) {
              setTotalVaultItems(data.totalVaultItems);
            }
            setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "success", text: `✅ [完成] 檔案 ${prependedName} 成功分析並歸類到知識庫！` }]);
          } else {
            throw new Error(data.error || "內部歸類錯誤");
          }
        }
        
        stats.successCount++;
        stats.processedFiles.push(fileInfo);
      } catch (fileErr: any) {
        stats.failCount++;
        const errMsg = fileErr.message || String(fileErr);
        if (targetMode === "import") {
          setBatchTexts(prev => prev.map(item => item.name === prependedName ? { 
            ...item, 
            status: "error", 
            error: errMsg 
          } : item));
        }
        setBatchLogs(prev => [...prev, { time: new Date().toLocaleTimeString(), type: "error", text: `❌ 檔案 ${prependedName} 處理失敗: ${errMsg}` }]);

--------------------------------------------------------------------------------

=== Line 937 | Step 831 | Tool replace_file_content ===
Description: Change handleImportLocalFiles to launch background parsing directly without opening the modal.
Instruction: Call runBatchIngestProcess directly inside handleImportLocalFiles.
TargetContent:
  const handleImportLocalFiles = (filesToImport: typeof scannedLocalFiles) => {
    if (filesToImport.length === 0) return;
    setBatchIngestFiles(filesToImport);
    setBatchIngestTargetMode("import");
    setBatchExecutionMode("all");
    setBatchIngestStatus("idle");
    setBatchProcessedCount(0);
    setBatchElapsedSeconds(0);
    setBatchLogs([]);
    setIsBatchIngestModalOpen(true);

ReplacementContent:
  const handleImportLocalFiles = (filesToImport: typeof scannedLocalFiles) => {
    if (filesToImport.length === 0) return;
    setBatchIngestFiles(filesToImport);
    setBatchIngestTargetMode("import");
    setBatchExecutionMode("all");
    setBatchLogs([]);
    runBatchIngestProcess(filesToImport, "import", "all", filesToImport.length, 5);
  };

--------------------------------------------------------------------------------

=== Line 941 | Step 835 | Tool replace_file_content ===
Description: Remove duplicate closing brace at handleImportLocalFiles.
Instruction: Remove duplicate brace.
TargetContent:
  };
  };

ReplacementContent:
  };

--------------------------------------------------------------------------------

=== Line 943 | Step 837 | Tool replace_file_content ===
Description: Implement conditional rendering based on status in the queue file card component.
Instruction: Insert conditional checks for status === 'pending', 'parsing', or 'error' in App.tsx queue render block.
TargetContent:
                           return (
                             <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs">
                               <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                 <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                 <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                               </div>

                               {/* Classification Field */}
                               <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                 <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                   <span>🏫 自動學術分類:</span>
                                   <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                     activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                     activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                     activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                     activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                     'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                   }`}>
                                     {activeCat}
                                   </span>
                                 </span>
                                 <div className="flex items-center gap-1.5">
                                   <span className="text-slate-500 font-sans">分類變更:</span>
                                   <select
                                     value={activeCat}
                                     onChange={(e) => {
                                       if (origIdx !== -1) {
                                         const newArr = [...batchTexts];
                                         newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                         setBatchTexts(newArr);
                                       }
                                     }}
                                     className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                   >
                                     <option value="判例">⚖️ 判例</option>
                                     <option value="證物">📁 證物</option>
                                     <option value="學術錄影">🎥 學術錄影</option>
                                     <option value="學術錄音">📻 學術錄音</option>
                                     <option value="學術教材">📖 學術教材</option>
                                   </select>
                                 </div>
                               </div>

                               {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                               {(() => {
                                 const detected = proofreadText(file.content);
                                 if (detected.length === 0) return null;
                                 return (
                                   <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                     <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                       <span className="flex items-center gap-1">
                                         <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                         ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                       </span>
                                       <button
                                         type="button"
                                         onClick={() => {
                                           if (origIdx !== -1) {
                                             const newArr = [...batchTexts];
                                             newArr[origIdx].content = autoCorrectText(file.content);
                                             setBatchTexts(newArr);
                                           }
                                         }}
                                         className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                       >
                                         <span>⚡ 一鍵本卡校正</span>
                                       </button>
                                     </div>
                                     <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5">
                                       {detected.map((match, mIdx) => (
                                         <span 
                                           key={match.id + '-' + mIdx} 
                                           className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                           title={`${match.reason} (建議修正為：${match.correct})`}
                                         >
                                           <span className="text-rose-455 line-through mr-1">{match.suspect}</span>
                                           <span className="text-slate-600">➔</span>
                                           <span className="text-emerald-400 font-bold ml-1">{match.correct}</span>
                                         </span>
                                       ))}
                                     </div>
                                     <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                       提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                     </p>
                                   </div>
                                 );
                               })()}

                               <textarea 
                                 value={file.content}
                                 onChange={(e) => {
                                   if (origIdx !== -1) {
                                     const newArr = [...batchTexts];
                                     newArr[origIdx].content = e.target.value;
                                     setBatchTexts(newArr);
                                   }
                                 }}
                                 rows={3}
                                 className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5"
                               />
                             </div>
                           );

ReplacementContent:
                            const status = file.status || "ready";
                            if (status === "pending") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950/40 border border-slate-800 border-dashed rounded-lg flex flex-col gap-2 text-xs">
                                  <div className="flex justify-between items-center text-[11px] font-mono text-slate-500 border-b border-slate-900/50 pb-1.5 mb-0.5">
                                    <span className="truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <button 
                                      onClick={() => setBatchTexts(prev => prev.filter(b => b.name !== file.name))}
                                      className="text-slate-600 hover:text-rose-400 bg-transparent border-none cursor-pointer p-0"
                                    >
                                      <X className="w-3.5 h-3.5" />
                                    </button>
                                  </div>
                                  <div className="flex items-center gap-2 py-2 text-slate-550 font-sans">
                                    <span className="animate-pulse">⏳</span>
                                    <span>排程中，等待 GPU ASR 語音轉譯/OCR 解析...</span>
                                  </div>
                                </div>
                              );
                            }

                            if (status === "parsing") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950/80 border border-amber-500/20 rounded-lg flex flex-col gap-2 text-xs relative overflow-hidden">
                                  <div className="absolute top-0 left-0 h-[2px] bg-amber-500 animate-[pulse_1.5s_infinite]" style={{ width: '45%' }} />
                                  <div className="flex justify-between items-center text-[11px] font-mono text-amber-500 border-b border-slate-850 pb-1.5 mb-0.5">
                                    <span className="truncate max-w-[260px] font-bold" title={file.name}>{file.name}</span>
                                    <span className="text-[9px] bg-amber-500/10 text-amber-400 px-1.5 py-0.5 rounded border border-amber-500/15">GPU 運算中</span>
                                  </div>
                                  <div className="flex items-center gap-2.5 py-3.5 text-slate-350 font-sans">
                                    <RefreshCw className="w-3.5 h-3.5 animate-spin text-amber-500" />
                                    <span>正在進行本機 GPU/FFmpeg 智慧提取中...</span>
                                  </div>
                                </div>
                              );
                            }

                            if (status === "error") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-rose-500/20 rounded-lg flex flex-col gap-2 text-xs">
                                  <div className="flex justify-between items-center text-[11px] font-mono text-rose-400 border-b border-slate-850 pb-1.5 mb-0.5">
                                    <span className="truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <button 
                                      onClick={() => setBatchTexts(prev => prev.filter(b => b.name !== file.name))}
                                      className="text-slate-550 hover:text-slate-300 bg-transparent border-none cursor-pointer p-0"
                                    >
                                      <X className="w-3.5 h-3.5" />
                                    </button>
                                  </div>
                                  <div className="flex flex-col gap-1 py-1.5 text-rose-450 font-sans">
                                    <span className="font-bold flex items-center gap-1">❌ 解析失敗：</span>
                                    <p className="text-[10px] text-slate-400 whitespace-pre-wrap bg-rose-500/5 p-2 rounded border border-rose-500/10 mt-1 font-mono leading-relaxed">
                                      {file.error || "未知錯誤"}
                                    </p>
                                  </div>
                                </div>
                              );
                            }

                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 自動學術分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-455 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">分類變更:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        if (origIdx !== -1) {
                                          const newArr = [...batchTexts];
                                          newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                          setBatchTexts(newArr);
                                        }
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(file.content);
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            if (origIdx !== -1) {
                                              const newArr = [...batchTexts];
                                              newArr[origIdx].content = autoCorrectText(file.content);
                                              setBatchTexts(newArr);
                                            }
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-455 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-400 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                      <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                        提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                      </p>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={file.content}
                                  onChange={(e) => {
                                    if (origIdx !== -1) {
                                      const newArr = [...batchTexts];
                                      newArr[origIdx].content = e.target.value;
                                      setBatchTexts(newArr);
                                    }
                                  }}
                                  rows={3}
                                  className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5"
                                />
                              </div>
                            );

--------------------------------------------------------------------------------

=== Line 958 | Step 853 | Tool replace_file_content ===
Description: Restore missing closing div tag in the Web Scraper section of App.tsx
Instruction: Add closing div tag for relative flex-1 container before the scrape button
TargetContent:
                        )}
                      <button
                        type="button"

ReplacementContent:
                        )}
                      </div>
                      <button
                        type="button"

--------------------------------------------------------------------------------

=== Line 1010 | Step 905 | Tool replace_file_content ===
Description: Add Clock icon to Lucide React imports
Instruction: Add Clock to the list of imported icons from lucide-react
TargetContent:
  SlidersHorizontal
} from "lucide-react";

ReplacementContent:
  SlidersHorizontal,
  Clock
} from "lucide-react";

--------------------------------------------------------------------------------

=== Line 1016 | Step 911 | Tool replace_file_content ===
Description: Set isLocalImporting to true when running local files import batch process
Instruction: Add setIsLocalImporting(true) if targetMode is import
TargetContent:
    if (targetMode === "import") {
      const placeholders: BatchFile[] = filesToProcess.map(f => ({

ReplacementContent:
    if (targetMode === "import") {
      setIsLocalImporting(true);
      const placeholders: BatchFile[] = filesToProcess.map(f => ({

--------------------------------------------------------------------------------

=== Line 1020 | Step 915 | Tool replace_file_content ===
Description: Add finally block to runBatchIngestProcess to set isLocalImporting to false
Instruction: Add finally block setting setIsLocalImporting(false) after the catch block of runBatchIngestProcess
TargetContent:
        }
      }
    }
  };

ReplacementContent:
        }
      }
    } finally {
      setIsLocalImporting(false);
    }
  };

--------------------------------------------------------------------------------

=== Line 1024 | Step 919 | Tool replace_file_content ===
Description: Update local file ingestion and import buttons to clearly state their behavior (with or without review)
Instruction: Rename local scan import/ingest buttons for clarity
TargetContent:
                                 <div className="flex gap-1.5">
                                   <button
                                     type="button"
                                     disabled={isLocalImporting}
                                     onClick={() => handleImportLocalFiles([file])}
                                     className="px-1.5 py-0.5 bg-slate-900 hover:bg-slate-800 border border-slate-800 text-slate-300 rounded text-[9px] cursor-pointer transition-colors"
                                   >
                                     📥 放入餵食區
                                   </button>
                                   <button
                                     type="button"
                                     disabled={isLocalImporting}
                                     onClick={() => handleDirectIngestLocalFiles([file])}
                                     className="px-1.5 py-0.5 bg-amber-500/10 hover:bg-amber-500/20 border border-amber-500/25 text-amber-400 rounded text-[9px] cursor-pointer transition-colors"
                                   >
                                     ⚡ 直接分析歸類
                                   </button>
                                 </div>
                               </div>
                             ))}
                           </div>

                           {/* Batch Action Buttons */}
                           <div className="flex gap-2 mt-2 pt-2 border-t border-slate-850">
                             <button
                               type="button"
                               disabled={isLocalImporting || filteredFiles.length === 0}
                               onClick={() => handleImportLocalFiles(filteredFiles)}
                               className="flex-1 py-1.5 bg-slate-900 border border-slate-800 hover:bg-slate-850 text-slate-300 font-semibold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                             >
                               📥 導入已篩選檔案 ({filteredFiles.length})
                             </button>
                             <button
                               type="button"
                               disabled={isLocalImporting || filteredFiles.length === 0}
                               onClick={() => handleDirectIngestLocalFiles(filteredFiles)}
                               className="flex-1 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 font-bold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                             >
                               ⚡ 分析已篩選檔案 ({filteredFiles.length})
                             </button>
                           </div>

ReplacementContent:
                                  <div className="flex gap-1.5">
                                   <button
                                     type="button"
                                     disabled={isLocalImporting}
                                     onClick={() => handleImportLocalFiles([file])}
                                     className="px-1.5 py-0.5 bg-slate-900 hover:bg-slate-800 border border-slate-800 text-slate-300 rounded text-[9px] cursor-pointer transition-colors"
                                   >
                                     📥 載入以進行校對
                                   </button>
                                   <button
                                     type="button"
                                     disabled={isLocalImporting}
                                     onClick={() => handleDirectIngestLocalFiles([file])}
                                     className="px-1.5 py-0.5 bg-amber-500/10 hover:bg-amber-500/20 border border-amber-500/25 text-amber-400 rounded text-[9px] cursor-pointer transition-colors"
                                   >
                                     ⚡ 快速解析直接入庫 (跳過校對)
                                   </button>
                                 </div>
                               </div>
                             ))}
                           </div>

                           {/* Batch Action Buttons */}
                           <div className="flex gap-2 mt-2 pt-2 border-t border-slate-850">
                             <button
                               type="button"
                               disabled={isLocalImporting || filteredFiles.length === 0}
                               onClick={() => handleImportLocalFiles(filteredFiles)}
                               className="flex-1 py-1.5 bg-slate-900 border border-slate-800 hover:bg-slate-850 text-slate-300 font-semibold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                             >
                               📥 載入已篩選檔案以進行校對 ({filteredFiles.length})
                             </button>
                             <button
                               type="button"
                               disabled={isLocalImporting || filteredFiles.length === 0}
                               onClick={() => handleDirectIngestLocalFiles(filteredFiles)}
                               className="flex-1 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 font-bold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                             >
                               ⚡ 快速解析直接入庫已篩選檔案 (跳過校對) ({filteredFiles.length})
                             </button>
                           </div>

--------------------------------------------------------------------------------

=== Line 1028 | Step 923 | Tool replace_file_content ===
Description: Rename single file import button to clear proofread intent
Instruction: Change 📥 放入餵食區 to 📥 載入以進行校對
TargetContent:
                                    className="px-1.5 py-0.5 bg-slate-900 hover:bg-slate-800 border border-slate-800 text-slate-300 rounded text-[9px] cursor-pointer transition-colors"
                                  >
                                    📥 放入餵食區
                                  </button>

ReplacementContent:
                                    className="px-1.5 py-0.5 bg-slate-900 hover:bg-slate-800 border border-slate-800 text-slate-300 rounded text-[9px] cursor-pointer transition-colors"
                                  >
                                    📥 載入以進行校對
                                  </button>

--------------------------------------------------------------------------------

=== Line 1030 | Step 925 | Tool replace_file_content ===
Description: Rename single file direct ingest button to specify skipping review
Instruction: Change ⚡ 直接分析歸類 to ⚡ 快速解析直接入庫 (跳過校對)
TargetContent:
                                    className="px-1.5 py-0.5 bg-amber-500/10 hover:bg-amber-500/20 border border-amber-500/25 text-amber-400 rounded text-[9px] cursor-pointer transition-colors"
                                  >
                                    ⚡ 直接分析歸類
                                  </button>

ReplacementContent:
                                    className="px-1.5 py-0.5 bg-amber-500/10 hover:bg-amber-500/20 border border-amber-500/25 text-amber-400 rounded text-[9px] cursor-pointer transition-colors"
                                  >
                                    ⚡ 快速解析直接入庫 (跳過校對)
                                  </button>

--------------------------------------------------------------------------------

=== Line 1032 | Step 927 | Tool replace_file_content ===
Description: Rename batch action buttons in local scanned files list
Instruction: Change batch import/analysis buttons labels for clarity
TargetContent:
                            <button
                              type="button"
                              disabled={isLocalImporting || filteredFiles.length === 0}
                              onClick={() => handleImportLocalFiles(filteredFiles)}
                              className="flex-1 py-1.5 bg-slate-900 border border-slate-800 hover:bg-slate-850 text-slate-300 font-semibold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                            >
                              📥 導入已篩選檔案 ({filteredFiles.length})
                            </button>
                            <button
                              type="button"
                              disabled={isLocalImporting || filteredFiles.length === 0}
                              onClick={() => handleDirectIngestLocalFiles(filteredFiles)}
                              className="flex-1 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 font-bold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                            >
                              ⚡ 分析已篩選檔案 ({filteredFiles.length})
                            </button>

ReplacementContent:
                            <button
                              type="button"
                              disabled={isLocalImporting || filteredFiles.length === 0}
                              onClick={() => handleImportLocalFiles(filteredFiles)}
                              className="flex-1 py-1.5 bg-slate-900 border border-slate-800 hover:bg-slate-850 text-slate-300 font-semibold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                            >
                              📥 載入已篩選檔案以進行校對 ({filteredFiles.length})
                            </button>
                            <button
                              type="button"
                              disabled={isLocalImporting || filteredFiles.length === 0}
                              onClick={() => handleDirectIngestLocalFiles(filteredFiles)}
                              className="flex-1 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 font-bold text-[10px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer disabled:opacity-50"
                            >
                              ⚡ 快速解析直接入庫已篩選檔案 (跳過校對) ({filteredFiles.length})
                            </button>

--------------------------------------------------------------------------------

=== Line 1040 | Step 935 | Tool replace_file_content ===
Description: Change queue bottom button label to highlight proofreading verification before DB ingestion
Instruction: Change button label to 🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫
TargetContent:
                      {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 開始全自動音檔翻譯、OCR 與自適應消化"}

ReplacementContent:
                      {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫"}

--------------------------------------------------------------------------------

=== Line 1044 | Step 939 | Tool replace_file_content ===
Description: Update placeholder text in the standby state card to match the new button label
Instruction: Change standby text to refer to the new button label: 確認校對無誤，開始智慧消化並寫入資料庫/智商庫
TargetContent:
                          待命狀態。請配置左方隊列並點擊下方「開始全自動音檔翻譯、OCR 與自適應消化」以啟動。

ReplacementContent:
                          待命狀態。請配置左方隊列並點擊下方「確認校對無誤，開始智慧消化並寫入資料庫/智商庫」以啟動。

--------------------------------------------------------------------------------

=== Line 1048 | Step 943 | Tool replace_file_content ===
Description: Add status interceptors for pending, parsing, and error files in the batchTexts queue card rendering loop
Instruction: Insert status-based conditional returns for pending, parsing, and error before returning the default ready card
TargetContent:
                        }).map((file, idx) => {
                          const origIdx = batchTexts.findIndex(b => b.name === file.name);
                          const activeCat = file.category || classifyFileCategory(file.name);
                          return (

ReplacementContent:
                        }).map((file, idx) => {
                          const origIdx = batchTexts.findIndex(b => b.name === file.name);
                          const activeCat = file.category || classifyFileCategory(file.name);

                          if (file.status === "pending") {
                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950/60 border border-dashed border-slate-850 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-slate-450 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">
                                    {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "等待中"} | UTF-8
                                  </span>
                                </div>
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                  <span className="text-slate-450 font-medium flex items-center gap-1.5">
                                    <span>🏫 預定分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                </div>
                                <div className="p-2 border border-slate-850/60 bg-slate-900/30 rounded-lg text-slate-450 text-[11px] font-sans flex items-center gap-2">
                                  <Clock className="w-3.5 h-3.5 text-slate-500 animate-pulse" />
                                  <span>⏳ 排程中，等待 GPU ASR 語音轉譯/OCR 解析...</span>
                                </div>
                              </div>
                            );
                          }

                          if (file.status === "parsing") {
                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950/80 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs ring-1 ring-cyan-500/10">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-cyan-455 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">
                                    {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "解析中"} | UTF-8
                                  </span>
                                </div>
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 預定分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                </div>
                                <div className="p-2 border border-cyan-500/20 bg-cyan-500/5 rounded-lg text-cyan-400 text-[11px] font-sans flex items-center gap-2">
                                  <RefreshCw className="w-3.5 h-3.5 text-cyan-400 animate-spin" />
                                  <span className="font-medium animate-pulse">🔄 正在進行本機 GPU/FFmpeg 智慧提取中...</span>
                                </div>
                              </div>
                            );
                          }

                          if (file.status === "error") {
                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-rose-950/40 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-rose-400 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">
                                    {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "失敗"} | UTF-8
                                  </span>
                                </div>
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 預定分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                </div>
                                <div className="p-2 border border-rose-500/20 bg-rose-550/5 rounded-lg text-rose-400 text-[11px] font-sans flex items-start gap-2">
                                  <AlertTriangle className="w-4 h-4 text-rose-400 shrink-0 mt-0.5" />
                                  <div className="flex-1 flex flex-col gap-1">
                                    <span className="font-bold">❌ 解析失敗：</span>
                                    <span className="font-mono text-[10px] text-rose-300 leading-normal break-all">{file.error || "未知錯誤"}</span>
                                  </div>
                                </div>
                              </div>
                            );
                          }

                          return (

--------------------------------------------------------------------------------

=== Line 1126 | Step 1021 | Tool replace_file_content ===
Description: Add state variables for managing the database explorer UI
Instruction: Add vault explorer state variables at line 424
TargetContent:
  const isBatchPausedRef = React.useRef(false);

ReplacementContent:
  // Digested Intelligence Vault Explorer States
  const [vaultItems, setVaultItems] = useState<any[]>([]);
  const [vaultSearchQuery, setVaultSearchQuery] = useState("");
  const [isVaultLoading, setIsVaultLoading] = useState(false);
  const [selectedVaultItem, setSelectedVaultItem] = useState<any>(null);
  const [isVaultEditMode, setIsVaultEditMode] = useState(false);
  const [vaultEditText, setVaultEditText] = useState("");
  const [vaultFilterCategory, setVaultFilterCategory] = useState("all");
  
  const isBatchPausedRef = React.useRef(false);

--------------------------------------------------------------------------------

=== Line 1134 | Step 1029 | Tool replace_file_content ===
Description: Add vault query, update, and delete helper functions and tab sync useEffect to App.tsx
Instruction: Add vault fetch, update, and delete operations below fetchSystemStatus
TargetContent:
  };

ReplacementContent:
  };

  // Fetch digested vault documents
  const fetchVaultItems = async () => {
    setIsVaultLoading(true);
    try {
      const resp = await fetch("/api/intelligence-vault");
      const data = await resp.json();
      if (data.status === "success") {
        setVaultItems(data.items);
      }
    } catch (e) {
      console.error("Failed to fetch vault items:", e);
    } finally {
      setIsVaultLoading(false);
    }
  };

  const deleteVaultItem = async (id: string) => {
    if (!confirm("確定要自智商庫/資料庫中永久刪除此教材與逐字稿嗎？此動作無法復原。")) return;
    try {
      const resp = await fetch(`/api/intelligence-vault/${id}`, {
        method: "DELETE"
      });
      const data = await resp.json();
      if (data.status === "success") {
        showAlert("✅ 教材已自智商庫中移除。");
        setSelectedVaultItem(null);
        fetchVaultItems();
        fetchSystemStatus(); // update DB counts
      } else {
        showAlert(`❌ 刪除失敗：${data.error}`);
      }
    } catch (e: any) {
      showAlert(`❌ 刪除發生錯誤：${e.message || String(e)}`);
    }
  };

  const updateVaultItem = async (id: string, text: string) => {
    try {
      const resp = await fetch(`/api/intelligence-vault/${id}`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ text })
      });
      const data = await resp.json();
      if (data.status === "success") {
        showAlert("✅ 教材內容更新成功！");
        setIsVaultEditMode(false);
        setSelectedVaultItem((prev: any) => prev ? { ...prev, text } : null);
        fetchVaultItems();
      } else {
        showAlert(`❌ 更新失敗：${data.error}`);
      }
    } catch (e: any) {
      showAlert(`❌ 更新發生錯誤：${e.message || String(e)}`);
    }
  };

  // Auto fetch vault items when activeTab changes to ingest
  useEffect(() => {
    if (activeTab === "ingest") {
      fetchVaultItems();
    }
  }, [activeTab]);

--------------------------------------------------------------------------------

=== Line 1142 | Step 1037 | Tool replace_file_content ===
Description: Call fetchVaultItems in handleBatchIngest when batch ingestion finishes
Instruction: Add fetchVaultItems() call after setIsIngesting(false)
TargetContent:
    setIsIngesting(false);
    setCurrentIngestingName("");

    const totalDurationSec = (Date.now() - startTime) / 1000;

ReplacementContent:
    setIsIngesting(false);
    setCurrentIngestingName("");
    fetchVaultItems();

    const totalDurationSec = (Date.now() - startTime) / 1000;

--------------------------------------------------------------------------------

=== Line 1150 | Step 1045 | Tool replace_file_content ===
Description: Refresh vault items list in runBatchIngestProcess when direct ingestion completes
Instruction: Add fetchVaultItems() call in the targetMode === "direct" check
TargetContent:
        if (targetMode === "direct") {
          fetchSystemStatus(); // Refresh DB stats
        }

ReplacementContent:
        if (targetMode === "direct") {
          fetchSystemStatus(); // Refresh DB stats
          fetchVaultItems();
        }

--------------------------------------------------------------------------------

=== Line 1160 | Step 1055 | Tool replace_file_content ===
Description: Insert database/intelligence vault explorer section into the batch Ingestion Tab
Instruction: Add the database explorer UI section at the end of the activeTab === 'ingest' block
TargetContent:
              </div>
            </div>
          )}

ReplacementContent:
              </div>

              {/* PERSISTENT DATABASE / INTELLIGENCE VAULT EXPLORER */}
              <div className="mt-8 pt-6 border-t border-slate-800 flex flex-col gap-4">
                <div className="flex justify-between items-center">
                  <div>
                    <h3 className="text-sm font-bold text-slate-100 flex items-center gap-2">
                      <span>📚</span> 已成功導入資料庫/智商庫之教材逐字稿列表 ({vaultItems.length} 個項目)
                    </h3>
                    <p className="text-[11px] text-slate-500 font-sans mt-0.5">
                      列出所有已持久化寫入資料庫/智商庫之教材，您可在此進行關鍵字快速搜尋、查看逐字稿全文與 AI 消化後的法理爭點，或進行編輯、刪除管理。
                    </p>
                  </div>
                  <button
                    type="button"
                    onClick={fetchVaultItems}
                    disabled={isVaultLoading}
                    className="px-2.5 py-1 bg-slate-900 hover:bg-slate-850 border border-slate-800 text-slate-350 rounded text-xs cursor-pointer transition-colors flex items-center gap-1.5"
                  >
                    <RefreshCw className={`w-3.5 h-3.5 ${isVaultLoading ? "animate-spin" : ""}`} />
                    <span>整理清單</span>
                  </button>
                </div>

                {/* Filter and Search Bar */}
                <div className="flex flex-col sm:flex-row gap-3 bg-slate-900/40 p-3 border border-slate-850 rounded-xl">
                  <div className="flex-1 relative">
                    <input
                      type="text"
                      value={vaultSearchQuery}
                      onChange={(e) => setVaultSearchQuery(e.target.value)}
                      placeholder="🔍 搜尋已存入之教材名稱或逐字稿關鍵字（如：民法、上課錄影、判例）..."
                      className="w-full bg-slate-950 border border-slate-850 focus:border-amber-500/80 rounded-lg py-2 pl-3 pr-8 text-xs text-slate-350 focus:outline-none transition-all font-sans"
                    />
                    {vaultSearchQuery && (
                      <button
                        type="button"
                        onClick={() => setVaultSearchQuery("")}
                        className="absolute right-2.5 top-2.5 text-slate-500 hover:text-slate-300 bg-transparent border-0 cursor-pointer"
                      >
                        <X className="w-3.5 h-3.5" />
                      </button>
                    )}
                  </div>
                </div>

                {/* Ingested Items Grid List */}
                {isVaultLoading ? (
                  <div className="py-8 text-center bg-slate-950/40 border border-slate-855 rounded-lg flex flex-col items-center justify-center gap-2">
                    <RefreshCw className="w-6 h-6 animate-spin text-amber-500" />
                    <p className="text-xs text-slate-500">正在加載智商庫教材清單中...</p>
                  </div>
                ) : (() => {
                  const filtered = vaultItems.filter(item => {
                    const q = vaultSearchQuery.toLowerCase().trim();
                    if (!q) return true;
                    return (
                      (item.source && item.source.toLowerCase().includes(q)) ||
                      (item.text && item.text.toLowerCase().includes(q)) ||
                      (item.category && item.category.toLowerCase().includes(q))
                    );
                  });

                  if (filtered.length === 0) {
                    return (
                      <div className="py-8 text-center bg-slate-950/40 border border-slate-850 border-dashed rounded-lg">
                        <p className="text-xs text-slate-550 italic">無符合關鍵字之已導入教材，或您尚未載入教材入庫。</p>
                      </div>
                    );
                  }

                  return (
                    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3.5 max-h-[380px] overflow-y-auto pr-1">
                      {filtered.map((item, vIdx) => {
                        const fileExt = item.source.split('.').pop()?.toUpperCase() || "TXT";
                        const cleanSnippet = item.text
                          ? item.text.replace(/【多模態餵養導入：[^】]+】\n?/, "").slice(0, 160) + "..."
                          : "無內容";
                        return (
                          <div 
                            key={item.id || vIdx} 
                            onClick={() => {
                              setSelectedVaultItem(item);
                              setVaultEditText(item.text || "");
                              setIsVaultEditMode(false);
                            }}
                            className="p-3 bg-slate-900 border border-slate-850 hover:border-amber-500/50 hover:bg-slate-900/80 rounded-xl transition-all flex flex-col gap-2.5 text-xs cursor-pointer group shadow-sm hover:shadow-md relative overflow-hidden"
                          >
                            <div className="absolute top-0 left-0 w-full h-[2.5px] bg-gradient-to-r from-amber-600 to-amber-700 opacity-60 group-hover:opacity-100 transition-opacity" />
                            <div className="flex justify-between items-start gap-1.5">
                              <span className="font-mono font-bold text-slate-200 group-hover:text-amber-500 transition-colors truncate flex-1" title={item.source}>
                                📄 {item.source}
                              </span>
                              <span className="px-1.5 py-0.5 bg-slate-950 border border-slate-850 text-[8px] font-bold text-slate-400 rounded shrink-0">
                                {fileExt}
                              </span>
                            </div>
                            <p className="text-[11px] text-slate-400 line-clamp-3 leading-relaxed font-sans mt-0.5">
                              {cleanSnippet}
                            </p>
                            <div className="mt-1 flex justify-between items-center text-[10px] border-t border-slate-855/60 pt-2 text-slate-500">
                              <span>字數: {item.text ? item.text.length : 0} 字</span>
                              <span className="text-amber-500 hover:text-amber-400 font-bold flex items-center gap-0.5">
                                🔍 點擊查看/校對 ➔
                              </span>
                            </div>
                          </div>
                        );
                      })}
                    </div>
                  );
                })()}
              </div>
            </div>
          )}

--------------------------------------------------------------------------------

=== Line 1168 | Step 1063 | Tool replace_file_content ===
Description: Insert selectedVaultItem detail and edit modal dialog before app container closing tag
Instruction: Insert selectedVaultItem modal block after Batch Ingest modal closing tag
TargetContent:
      </AnimatePresence>

    </div>
  );
}

ReplacementContent:
      </AnimatePresence>

      {/* Vault Item detail & edit Modal */}
      <AnimatePresence>
        {selectedVaultItem && (
          <div className="fixed inset-0 z-50 flex items-center justify-center p-4">
            <motion.div
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              exit={{ opacity: 0 }}
              onClick={() => setSelectedVaultItem(null)}
              className="absolute inset-0 bg-slate-955/85 backdrop-blur-sm"
            />
            <motion.div
              initial={{ scale: 0.95, y: 15, opacity: 0 }}
              animate={{ scale: 1, y: 0, opacity: 1 }}
              exit={{ scale: 0.95, y: 15, opacity: 0 }}
              transition={{ type: "spring", stiffness: 350, damping: 25 }}
              className="relative w-full max-w-3xl bg-slate-900 border border-slate-800 rounded-2xl p-6 shadow-2xl overflow-hidden text-slate-100 ring-1 ring-amber-500/20 max-h-[90vh] flex flex-col z-10"
            >
              <div className="absolute top-0 left-0 w-full h-[3px] bg-gradient-to-r from-amber-650 to-amber-750" />
              
              <div className="flex justify-between items-center mb-4 border-b border-slate-800 pb-3">
                <div>
                  <span className="px-1.5 py-0.5 bg-slate-950 border border-slate-800 text-[9px] font-bold text-amber-500 rounded uppercase tracking-wider mb-1 inline-block">
                    智商庫教材逐字稿資料
                  </span>
                  <h3 className="text-sm font-bold text-white flex items-center gap-2 truncate max-w-[500px]">
                    📄 {selectedVaultItem.source}
                  </h3>
                </div>
                <button
                  type="button"
                  onClick={() => setSelectedVaultItem(null)}
                  className="text-slate-450 hover:text-slate-100 transition-colors p-1 cursor-pointer border-none bg-transparent"
                >
                  <X className="w-4 h-4" />
                </button>
              </div>

              <div className="flex-1 overflow-y-auto mb-4 space-y-3 font-sans pr-1">
                {isVaultEditMode ? (
                  <div className="flex flex-col gap-2">
                    <label className="text-[11px] font-bold text-slate-400">✏️ 編輯校對教材與逐字稿文字內容：</label>
                    <textarea
                      value={vaultEditText}
                      onChange={(e) => setVaultEditText(e.target.value)}
                      rows={18}
                      className="w-full bg-slate-950 border border-slate-850 rounded-lg p-3 text-xs text-slate-200 focus:outline-none focus:border-amber-500/80 font-mono leading-relaxed"
                    />
                  </div>
                ) : (
                  <div className="bg-slate-955 border border-slate-850 p-4 rounded-xl text-xs leading-relaxed text-slate-350 font-mono whitespace-pre-wrap select-text h-[50vh] overflow-y-auto">
                    {selectedVaultItem.text}
                  </div>
                )}
              </div>

              <div className="flex justify-between items-center border-t border-slate-800 pt-4 mt-2">
                <button
                  type="button"
                  onClick={() => deleteVaultItem(selectedVaultItem.id)}
                  className="px-3.5 py-2 bg-rose-500/10 hover:bg-rose-500 border border-rose-500/25 text-rose-400 hover:text-white transition-all text-xs font-semibold rounded-lg flex items-center gap-1.5 cursor-pointer"
                >
                  🗑️ 自智商庫永久刪除
                </button>

                <div className="flex gap-2">
                  {isVaultEditMode ? (
                    <>
                      <button
                        type="button"
                        onClick={() => setIsVaultEditMode(false)}
                        className="px-4 py-2 bg-slate-950 hover:bg-slate-850 border border-slate-800 text-slate-400 text-xs font-semibold rounded-lg transition-colors cursor-pointer"
                      >
                        取消
                      </button>
                      <button
                        type="button"
                        onClick={() => updateVaultItem(selectedVaultItem.id, vaultEditText)}
                        className="px-4 py-2 bg-amber-500 hover:bg-amber-600 text-slate-950 text-xs font-bold rounded-lg transition-all cursor-pointer"
                      >
                        💾 儲存修改
                      </button>
                    </>
                  ) : (
                    <>
                      <button
                        type="button"
                        onClick={() => setIsVaultEditMode(true)}
                        className="px-4 py-2 bg-slate-950 hover:bg-slate-850 border border-slate-850 text-slate-300 text-xs font-semibold rounded-lg transition-colors cursor-pointer flex items-center gap-1.5"
                      >
                        ✏️ 編輯內容
                      </button>
                      <button
                        type="button"
                        onClick={() => {
                          navigator.clipboard.writeText(selectedVaultItem.text || "");
                          showAlert("✅ 已成功將逐字稿全文複製至剪貼簿！");
                        }}
                        className="px-4 py-2 bg-amber-500 hover:bg-amber-600 text-slate-950 text-xs font-bold rounded-lg transition-all cursor-pointer flex items-center gap-1.5"
                      >
                        📋 複製全文
                      </button>
                    </>
                  )}
                </div>
              </div>
            </motion.div>
          </div>
        )}
      </AnimatePresence>

    </div>
  );
}

--------------------------------------------------------------------------------

=== Line 1215 | Step 1110 | Tool replace_file_content ===
Description: Add queueTab state variable for queue panel mode switching
Instruction: Add queueTab state variable initialized to 'pending'
TargetContent:
  const [vaultFilterCategory, setVaultFilterCategory] = useState("all");

ReplacementContent:
  const [vaultFilterCategory, setVaultFilterCategory] = useState("all");
  const [queueTab, setQueueTab] = useState<"pending" | "vault">("pending");

--------------------------------------------------------------------------------

=== Line 1219 | Step 1114 | Tool replace_file_content ===
Description: Integrate queueTab switcher header in the queue panel in App.tsx
Instruction: Replace the static queue header with a toggle switch between Pending Queue and Ingested Vault
TargetContent:
                  <div className="p-4 bg-slate-900/80 border border-slate-800 rounded-xl flex flex-col gap-3">
                    <h3 className="text-xs font-semibold text-slate-300 tracking-wider flex items-center justify-between">
                      <span>📂 待處理的本地大文件隊列 ({batchTexts.length} 個項目)</span>
                      {batchTexts.length > 0 && (
                        <button 
                          onClick={() => {
                            if (confirm("確定要清空材料隊列嗎？")) {
                              setBatchTexts([]);
                            }
                          }}
                          className="text-[10px] text-rose-400 hover:text-rose-300 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent"
                        >
                          <Trash2 className="w-3 h-3" /> 清空隊列
                        </button>
                      )}
                    </h3>

ReplacementContent:
                  <div className="p-4 bg-slate-900/80 border border-slate-800 rounded-xl flex flex-col gap-3">
                    <div className="flex border-b border-slate-800 pb-1.5 justify-between items-center text-xs">
                      <div className="flex gap-4">
                        <button
                          type="button"
                          onClick={() => setQueueTab("pending")}
                          className={`pb-2 font-bold transition-all relative border-b-2 cursor-pointer ${
                            queueTab === "pending"
                              ? "text-amber-500 border-amber-500"
                              : "text-slate-400 border-transparent hover:text-slate-200"
                          }`}
                        >
                          ⏳ 待校對隊列 ({batchTexts.length})
                        </button>
                        <button
                          type="button"
                          onClick={() => {
                            setQueueTab("vault");
                            fetchVaultItems();
                          }}
                          className={`pb-2 font-bold transition-all relative border-b-2 cursor-pointer ${
                            queueTab === "vault"
                              ? "text-amber-500 border-amber-500"
                              : "text-slate-400 border-transparent hover:text-slate-200"
                          }`}
                        >
                          📚 已入庫智商庫 ({vaultItems.length})
                        </button>
                      </div>

                      {queueTab === "pending" && batchTexts.length > 0 && (
                        <button 
                          onClick={() => {
                            if (confirm("確定要清空待處理校對材料隊列嗎？")) {
                              setBatchTexts([]);
                            }
                          }}
                          className="text-[10px] text-rose-400 hover:text-rose-350 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent mb-2"
                        >
                          <Trash2 className="w-3.5 h-3.5" /> 清空隊列
                        </button>
                      )}
                      {queueTab === "vault" && (
                        <button 
                          onClick={() => fetchVaultItems()}
                          disabled={isVaultLoading}
                          className="text-[10px] text-amber-500 hover:text-amber-400 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent mb-2 disabled:opacity-50"
                        >
                          <RefreshCw className={`w-3 h-3 ${isVaultLoading ? "animate-spin" : ""}`} /> 整理清單
                        </button>
                      )}
                    </div>

--------------------------------------------------------------------------------

=== Line 1223 | Step 1118 | Tool replace_file_content ===
Description: Restrict LanguageTool bulk correction banner to Pending Queue tab only
Instruction: Wrap bulk correction banner in a check for queueTab === 'pending'
TargetContent:
                    {batchTexts.length > 0 && (
                      <div className="bg-amber-500/10 border border-amber-500/15 p-3 rounded-lg flex flex-col sm:flex-row items-center justify-between gap-3 text-xs">
                        <div className="flex items-center gap-2">
                          <span className="p-1 px-1.5 bg-amber-500/20 text-amber-400 font-bold rounded text-[9px] border border-amber-500/10 shrink-0 font-mono">
                            OFFLINE ENGINE
                          </span>
                          <p className="text-[11px] text-slate-300 font-sans leading-relaxed">
                            <b>LanguageTool 離線法律校對核心：</b>已自動在背景套用繁體法律術語和語音 (ASR) 混淆音判定規則。
                          </p>
                        </div>
                        <button
                          type="button"
                          onClick={() => {
                            const updated = batchTexts.map(f => ({
                              ...f,
                              content: autoCorrectText(f.content)
                            }));
                            setBatchTexts(updated);
                          }}
                          className="w-full sm:w-auto px-3 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 rounded-lg font-bold text-[11px] transition-all flex items-center justify-center gap-1 shrink-0 border-none cursor-pointer"
                        >
                          <CheckCircle className="w-3.5 h-3.5 text-slate-950" />
                          一鍵校對修正全體隊列
                        </button>
                      </div>
                    )}

ReplacementContent:
                    {queueTab === "pending" && batchTexts.length > 0 && (
                      <div className="bg-amber-500/10 border border-amber-500/15 p-3 rounded-lg flex flex-col sm:flex-row items-center justify-between gap-3 text-xs">
                        <div className="flex items-center gap-2">
                          <span className="p-1 px-1.5 bg-amber-500/20 text-amber-400 font-bold rounded text-[9px] border border-amber-500/10 shrink-0 font-mono">
                            OFFLINE ENGINE
                          </span>
                          <p className="text-[11px] text-slate-300 font-sans leading-relaxed">
                            <b>LanguageTool 離線法律校對核心：</b>已自動在背景套用繁體法律術語和語音 (ASR) 混淆音判定規則。
                          </p>
                        </div>
                        <button
                          type="button"
                          onClick={() => {
                            const updated = batchTexts.map(f => ({
                              ...f,
                              content: autoCorrectText(f.content)
                            }));
                            setBatchTexts(updated);
                          }}
                          className="w-full sm:w-auto px-3 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 rounded-lg font-bold text-[11px] transition-all flex items-center justify-center gap-1 shrink-0 border-none cursor-pointer"
                        >
                          <CheckCircle className="w-3.5 h-3.5 text-slate-950" />
                          一鍵校對修正全體隊列
                        </button>
                      </div>
                    )}

--------------------------------------------------------------------------------

=== Line 1282 | Step 1110 | Tool replace_file_content ===
Description: Add queueTab state variable for queue panel mode switching
Instruction: Add queueTab state variable initialized to 'pending'
TargetContent:
  const [vaultFilterCategory, setVaultFilterCategory] = useState("all");

ReplacementContent:
  const [vaultFilterCategory, setVaultFilterCategory] = useState("all");
  const [queueTab, setQueueTab] = useState<"pending" | "vault">("pending");

--------------------------------------------------------------------------------

=== Line 1286 | Step 1114 | Tool replace_file_content ===
Description: Integrate queueTab switcher header in the queue panel in App.tsx
Instruction: Replace the static queue header with a toggle switch between Pending Queue and Ingested Vault
TargetContent:
                  <div className="p-4 bg-slate-900/80 border border-slate-800 rounded-xl flex flex-col gap-3">
                    <h3 className="text-xs font-semibold text-slate-300 tracking-wider flex items-center justify-between">
                      <span>📂 待處理的本地大文件隊列 ({batchTexts.length} 個項目)</span>
                      {batchTexts.length > 0 && (
                        <button 
                          onClick={() => {
                            if (confirm("確定要清空材料隊列嗎？")) {
                              setBatchTexts([]);
                            }
                          }}
                          className="text-[10px] text-rose-400 hover:text-rose-300 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent"
                        >
                          <Trash2 className="w-3 h-3" /> 清空隊列
                        </button>
                      )}
                    </h3>

ReplacementContent:
                  <div className="p-4 bg-slate-900/80 border border-slate-800 rounded-xl flex flex-col gap-3">
                    <div className="flex border-b border-slate-800 pb-1.5 justify-between items-center text-xs">
                      <div className="flex gap-4">
                        <button
                          type="button"
                          onClick={() => setQueueTab("pending")}
                          className={`pb-2 font-bold transition-all relative border-b-2 cursor-pointer ${
                            queueTab === "pending"
                              ? "text-amber-500 border-amber-500"
                              : "text-slate-400 border-transparent hover:text-slate-200"
                          }`}
                        >
                          ⏳ 待校對隊列 ({batchTexts.length})
                        </button>
                        <button
                          type="button"
                          onClick={() => {
                            setQueueTab("vault");
                            fetchVaultItems();
                          }}
                          className={`pb-2 font-bold transition-all relative border-b-2 cursor-pointer ${
                            queueTab === "vault"
                              ? "text-amber-500 border-amber-500"
                              : "text-slate-400 border-transparent hover:text-slate-200"
                          }`}
                        >
                          📚 已入庫智商庫 ({vaultItems.length})
                        </button>
                      </div>

                      {queueTab === "pending" && batchTexts.length > 0 && (
                        <button 
                          onClick={() => {
                            if (confirm("確定要清空待處理校對材料隊列嗎？")) {
                              setBatchTexts([]);
                            }
                          }}
                          className="text-[10px] text-rose-400 hover:text-rose-350 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent mb-2"
                        >
                          <Trash2 className="w-3.5 h-3.5" /> 清空隊列
                        </button>
                      )}
                      {queueTab === "vault" && (
                        <button 
                          onClick={() => fetchVaultItems()}
                          disabled={isVaultLoading}
                          className="text-[10px] text-amber-500 hover:text-amber-400 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent mb-2 disabled:opacity-50"
                        >
                          <RefreshCw className={`w-3 h-3 ${isVaultLoading ? "animate-spin" : ""}`} /> 整理清單
                        </button>
                      )}
                    </div>

--------------------------------------------------------------------------------

=== Line 1290 | Step 1118 | Tool replace_file_content ===
Description: Restrict LanguageTool bulk correction banner to Pending Queue tab only
Instruction: Wrap bulk correction banner in a check for queueTab === 'pending'
TargetContent:
                    {batchTexts.length > 0 && (
                      <div className="bg-amber-500/10 border border-amber-500/15 p-3 rounded-lg flex flex-col sm:flex-row items-center justify-between gap-3 text-xs">
                        <div className="flex items-center gap-2">
                          <span className="p-1 px-1.5 bg-amber-500/20 text-amber-400 font-bold rounded text-[9px] border border-amber-500/10 shrink-0 font-mono">
                            OFFLINE ENGINE
                          </span>
                          <p className="text-[11px] text-slate-300 font-sans leading-relaxed">
                            <b>LanguageTool 離線法律校對核心：</b>已自動在背景套用繁體法律術語和語音 (ASR) 混淆音判定規則。
                          </p>
                        </div>
                        <button
                          type="button"
                          onClick={() => {
                            const updated = batchTexts.map(f => ({
                              ...f,
                              content: autoCorrectText(f.content)
                            }));
                            setBatchTexts(updated);
                          }}
                          className="w-full sm:w-auto px-3 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 rounded-lg font-bold text-[11px] transition-all flex items-center justify-center gap-1 shrink-0 border-none cursor-pointer"
                        >
                          <CheckCircle className="w-3.5 h-3.5 text-slate-950" />
                          一鍵校對修正全體隊列
                        </button>
                      </div>
                    )}

ReplacementContent:
                    {queueTab === "pending" && batchTexts.length > 0 && (
                      <div className="bg-amber-500/10 border border-amber-500/15 p-3 rounded-lg flex flex-col sm:flex-row items-center justify-between gap-3 text-xs">
                        <div className="flex items-center gap-2">
                          <span className="p-1 px-1.5 bg-amber-500/20 text-amber-400 font-bold rounded text-[9px] border border-amber-500/10 shrink-0 font-mono">
                            OFFLINE ENGINE
                          </span>
                          <p className="text-[11px] text-slate-300 font-sans leading-relaxed">
                            <b>LanguageTool 離線法律校對核心：</b>已自動在背景套用繁體法律術語和語音 (ASR) 混淆音判定規則。
                          </p>
                        </div>
                        <button
                          type="button"
                          onClick={() => {
                            const updated = batchTexts.map(f => ({
                              ...f,
                              content: autoCorrectText(f.content)
                            }));
                            setBatchTexts(updated);
                          }}
                          className="w-full sm:w-auto px-3 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 rounded-lg font-bold text-[11px] transition-all flex items-center justify-center gap-1 shrink-0 border-none cursor-pointer"
                        >
                          <CheckCircle className="w-3.5 h-3.5 text-slate-950" />
                          一鍵校對修正全體隊列
                        </button>
                      </div>
                    )}

--------------------------------------------------------------------------------

=== Line 1344 | Step 1172 | Tool replace_file_content ===
Description: Extend updateVaultItem to support category parameter in API body.
Instruction: Modify updateVaultItem in App.tsx to support the category parameter.
TargetContent:
  const updateVaultItem = async (id: string, text: string) => {
    try {
      const resp = await fetch(`/api/intelligence-vault/${id}`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ text })
      });
      const data = await resp.json();
      if (data.status === "success") {
        showAlert("✅ 教材內容更新成功！");
        setIsVaultEditMode(false);
        setSelectedVaultItem((prev: any) => prev ? { ...prev, text } : null);
        fetchVaultItems();
      } else {
        showAlert(`❌ 更新失敗：${data.error}`);
      }
    } catch (e: any) {
      showAlert(`❌ 更新發生錯誤：${e.message || String(e)}`);
    }
  };

ReplacementContent:
  const updateVaultItem = async (id: string, text: string, category?: string) => {
    try {
      const resp = await fetch(`/api/intelligence-vault/${id}`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ text, category })
      });
      const data = await resp.json();
      if (data.status === "success") {
        showAlert("✅ 教材內容更新成功！");
        setIsVaultEditMode(false);
        setSelectedVaultItem((prev: any) => prev ? { ...prev, text, category: category || prev.category } : null);
        fetchVaultItems();
      } else {
        showAlert(`❌ 更新失敗：${data.error}`);
      }
    } catch (e: any) {
      showAlert(`❌ 更新發生錯誤：${e.message || String(e)}`);
    }
  };

--------------------------------------------------------------------------------

=== Line 1348 | Step 1176 | Tool replace_file_content ===
Description: Dynamically calculate filter counts and add vault search box in the queue panel.
Instruction: Update the Category Filter Selector and add vaultSearchQuery input conditional on queueTab.
TargetContent:
                    {/* Category Filter selector */}
                    <div className="bg-slate-950 border border-slate-850 rounded-lg p-2.5 flex flex-col gap-1.5">
                      <div className="text-[11px] text-slate-400 font-semibold tracking-wider flex items-center justify-between font-sans">
                        <span className="flex items-center gap-1 text-slate-300">🔍 教材標籤篩選器</span>
                        <span className="text-[10px] text-slate-500 font-mono">
                          ⚖️ 判例({batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === '判例').length}) | 
                          📁 證物({batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === '證物').length}) | 
                          🎥 影音({batchTexts.filter(b => ['學術錄影', '學術錄音'].includes(b.category || classifyFileCategory(b.name) || '')).length})
                        </span>
                      </div>
                      <div className="flex flex-wrap gap-1.5 mt-1">
                        {[
                          { id: "all", label: "全部顯示", emoji: "🗂️" },
                          { id: "判例", label: "⚖️ 判例", emoji: "" },
                          { id: "證物", label: "📁 證物", emoji: "" },
                          { id: "學術錄影", label: "🎥 學術錄影", emoji: "" },
                          { id: "學術錄音", label: "📻 學術錄音", emoji: "" },
                          { id: "學術教材", label: "📖 學術教材", emoji: "" }
                        ].map((btn) => {
                          const count = btn.id === "all" ? batchTexts.length : batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === btn.id).length;
                          const isActive = ingestFilter === btn.id;
                          return (
                            <button
                              key={btn.id}
                              type="button"
                              onClick={() => setIngestFilter(btn.id)}
                              className={`px-2.5 py-1 text-[11px] font-semibold rounded-md border transition-all cursor-pointer flex items-center gap-1 ${
                                isActive 
                                  ? "bg-amber-600/20 text-amber-450 border-amber-500/50 shadow-sm"
                                  : "bg-slate-900 text-slate-400 border-slate-800 hover:text-slate-200 hover:border-slate-700"
                              }`}
                            >
                              <span>{btn.emoji} {btn.label}</span>
                              <span className={`text-[10px] font-mono px-1 rounded-sm ${isActive ? "bg-amber-400/20 text-amber-300" : "bg-slate-950 text-slate-500"}`}>{count}</span>
                            </button>
                          );
                        })}
                      </div>
                    </div>

ReplacementContent:
                    {/* Category Filter selector */}
                    <div className="bg-slate-950 border border-slate-850 rounded-lg p-2.5 flex flex-col gap-1.5">
                      <div className="text-[11px] text-slate-400 font-semibold tracking-wider flex items-center justify-between font-sans">
                        <span className="flex items-center gap-1 text-slate-300">🔍 教材標籤篩選器</span>
                        <span className="text-[10px] text-slate-500 font-mono">
                          {queueTab === "pending" ? (
                            <>
                              ⚖️ 判例({batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === '判例').length}) | 
                              📁 證物({batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === '證物').length}) | 
                              🎥 影音({batchTexts.filter(b => ['學術錄影', '學術錄音'].includes(b.category || classifyFileCategory(b.name) || '')).length})
                            </>
                          ) : (
                            <>
                              ⚖️ 判例({vaultItems.filter(b => (b.category || classifyFileCategory(b.source)) === '判例').length}) | 
                              📁 證物({vaultItems.filter(b => (b.category || classifyFileCategory(b.source)) === '證物').length}) | 
                              🎥 影音({vaultItems.filter(b => ['學術錄影', '學術錄音'].includes(b.category || classifyFileCategory(b.source) || '')).length})
                            </>
                          )}
                        </span>
                      </div>
                      <div className="flex flex-wrap gap-1.5 mt-1">
                        {[
                          { id: "all", label: "全部顯示", emoji: "🗂️" },
                          { id: "判例", label: "⚖️ 判例", emoji: "" },
                          { id: "證物", label: "📁 證物", emoji: "" },
                          { id: "學術錄影", label: "🎥 學術錄影", emoji: "" },
                          { id: "學術錄音", label: "📻 學術錄音", emoji: "" },
                          { id: "學術教材", label: "📖 學術教材", emoji: "" }
                        ].map((btn) => {
                          const count = btn.id === "all"
                            ? (queueTab === "pending" ? batchTexts.length : vaultItems.length)
                            : (queueTab === "pending"
                                ? batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === btn.id).length
                                : vaultItems.filter(b => (b.category || classifyFileCategory(b.source)) === btn.id).length
                              );
                          const isActive = ingestFilter === btn.id;
                          return (
                            <button
                              key={btn.id}
                              type="button"
                              onClick={() => setIngestFilter(btn.id)}
                              className={`px-2.5 py-1 text-[11px] font-semibold rounded-md border transition-all cursor-pointer flex items-center gap-1 ${
                                isActive 
                                  ? "bg-amber-600/20 text-amber-455 border-amber-500/50 shadow-sm"
                                  : "bg-slate-900 text-slate-400 border-slate-800 hover:text-slate-200 hover:border-slate-700"
                              }`}
                            >
                              <span>{btn.emoji} {btn.label}</span>
                              <span className={`text-[10px] font-mono px-1 rounded-sm ${isActive ? "bg-amber-400/20 text-amber-300" : "bg-slate-950 text-slate-500"}`}>{count}</span>
                            </button>
                          );
                        })}
                      </div>
                    </div>

                    {/* Vault Search Input (only when on vault tab) */}
                    {queueTab === "vault" && (
                      <div className="relative mt-1">
                        <input
                          type="text"
                          value={vaultSearchQuery}
                          onChange={(e) => setVaultSearchQuery(e.target.value)}
                          placeholder="🔍 搜尋已存入之教材名稱或逐字稿關鍵字..."
                          className="w-full bg-slate-950 border border-slate-850 focus:border-amber-500/80 rounded-lg py-1.5 pl-3 pr-8 text-xs text-slate-350 focus:outline-none transition-all font-sans"
                        />
                        {vaultSearchQuery && (
                          <button
                            type="button"
                            onClick={() => setVaultSearchQuery("")}
                            className="absolute right-2.5 top-2 text-slate-500 hover:text-slate-300 bg-transparent border-0 cursor-pointer"
                          >
                            <X className="w-3 h-3" />
                          </button>
                        )}
                      </div>
                    )}

--------------------------------------------------------------------------------

=== Line 1354 | Step 1182 | Tool replace_file_content ===
Description: Integrate pending and vault card lists with full editing options in the queue panel.
Instruction: Conditionally render batchTexts queue cards or vaultItems persistent cards based on queueTab, and conditionally display the batch ingest button.
TargetContent:
                    <div className="space-y-3.5 max-h-[480px] overflow-y-auto pr-1">
                      {batchTexts.filter(file => {
                        if (ingestFilter === "all") return true;
                        const fileCat = file.category || classifyFileCategory(file.name);
                        return fileCat === ingestFilter;
                      }).length === 0 ? (
                        <div className="text-center py-8 bg-slate-950/40 border border-slate-850 rounded-lg">
                          <p className="text-xs text-slate-500 italic">此分類篩選下無任何檔案項目。</p>
                        </div>
                      ) : (
                        batchTexts.filter(file => {
                          if (ingestFilter === "all") return true;
                          const fileCat = file.category || classifyFileCategory(file.name);
                          return fileCat === ingestFilter;
                        }).map((file, idx) => {
                          const origIdx = batchTexts.findIndex(b => b.name === file.name);
                          const activeCat = file.category || classifyFileCategory(file.name);

                          if (file.status === "pending") {
                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950/60 border border-dashed border-slate-850 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-slate-450 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">
                                    {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "等待中"} | UTF-8
                                  </span>
                                </div>
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                  <span className="text-slate-450 font-medium flex items-center gap-1.5">
                                    <span>🏫 預定分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </div>
                                  <div className="p-2 border border-slate-850/60 bg-slate-900/30 rounded-lg text-slate-450 text-[11px] font-sans flex items-center gap-2">
                                    <Clock className="w-3.5 h-3.5 text-slate-500 animate-pulse" />
                                    <span>⏳ 排程中，等待 GPU ASR 語音轉譯/OCR 解析...</span>
                                  </div>
                                </div>
                              );
                            }

                            if (file.status === "parsing") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950/80 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs ring-1 ring-cyan-500/10">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-cyan-455 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "解析中"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-cyan-500/20 bg-cyan-500/5 rounded-lg text-cyan-400 text-[11px] font-sans flex items-center gap-2">
                                    <RefreshCw className="w-3.5 h-3.5 text-cyan-400 animate-spin" />
                                    <span className="font-medium animate-pulse">🔄 正在進行本機 GPU/FFmpeg 智慧提取中...</span>
                                  </div>
                                </div>
                              );
                            }

                            if (file.status === "error") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-rose-950/40 rounded-lg flex flex-col gap-2.5 text-xs">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-rose-400 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "失敗"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-450 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-rose-500/20 bg-rose-550/5 rounded-lg text-rose-400 text-[11px] font-sans flex items-start gap-2">
                                    <AlertTriangle className="w-4 h-4 text-rose-400 shrink-0 mt-0.5" />
                                    <div className="flex-1 flex flex-col gap-1">
                                      <span className="font-bold">❌ 解析失敗：</span>
                                      <span className="font-mono text-[10px] text-rose-300 leading-normal break-all">{file.error || "未知錯誤"}</span>
                                    </div>
                                  </div>
                                </div>
                              );
                            }

                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-955 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 自動學術分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">分類變更:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        if (origIdx !== -1) {
                                          const newArr = [...batchTexts];
                                          newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                          setBatchTexts(newArr);
                                        }
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(file.content);
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            if (origIdx !== -1) {
                                              const newArr = [...batchTexts];
                                              newArr[origIdx].content = autoCorrectText(file.content);
                                              setBatchTexts(newArr);
                                            }
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-450 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-400 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                      <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                        提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                      </p>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={file.content}
                                  onChange={(e) => {
                                    if (origIdx !== -1) {
                                      const newArr = [...batchTexts];
                                      newArr[origIdx].content = e.target.value;
                                      setBatchTexts(newArr);
                                    }
                                  }}
                                  rows={3}
                                  className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-mono text-[11px]"
                                />
                              </div>
                            );
                          })
                        )
                      )}
                    </div>

                    <button 
                      onClick={handleBatchIngest}
                      disabled={isIngesting}
                      className="w-full py-3 bg-gradient-to-r from-amber-600 to-amber-700 text-white rounded-lg font-semibold hover:brightness-110 active:brightness-95 transition-all cursor-pointer text-xs flex items-center justify-center gap-2"
                    >
                      {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫"}
                    </button>

ReplacementContent:
                    <div className="space-y-3.5 max-h-[480px] overflow-y-auto pr-1">
                      {queueTab === "pending" ? (
                        batchTexts.filter(file => {
                          if (ingestFilter === "all") return true;
                          const fileCat = file.category || classifyFileCategory(file.name);
                          return fileCat === ingestFilter;
                        }).length === 0 ? (
                          <div className="text-center py-8 bg-slate-950/40 border border-slate-850 rounded-lg">
                            <p className="text-xs text-slate-500 italic">此分類篩選下無任何檔案項目。</p>
                          </div>
                        ) : (
                          batchTexts.filter(file => {
                            if (ingestFilter === "all") return true;
                            const fileCat = file.category || classifyFileCategory(file.name);
                            return fileCat === ingestFilter;
                          }).map((file, idx) => {
                            const origIdx = batchTexts.findIndex(b => b.name === file.name);
                            const activeCat = file.category || classifyFileCategory(file.name);

                            if (file.status === "pending") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950/60 border border-dashed border-slate-850 rounded-lg flex flex-col gap-2.5 text-xs">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-slate-455 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "等待中"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-450 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-slate-850/60 bg-slate-900/30 rounded-lg text-slate-450 text-[11px] font-sans flex items-center gap-2">
                                    <Clock className="w-3.5 h-3.5 text-slate-500 animate-pulse" />
                                    <span>⏳ 排程中，等待 GPU ASR 語音轉譯/OCR 解析...</span>
                                  </div>
                                </div>
                              );
                            }

                            if (file.status === "parsing") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950/80 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs ring-1 ring-cyan-500/10">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-cyan-455 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "解析中"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-455 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-cyan-500/20 bg-cyan-500/5 rounded-lg text-cyan-400 text-[11px] font-sans flex items-center gap-2">
                                    <RefreshCw className="w-3.5 h-3.5 text-cyan-400 animate-spin" />
                                    <span className="font-medium animate-pulse">🔄 正在進行本機 GPU/FFmpeg 智慧提取中...</span>
                                  </div>
                                </div>
                              );
                            }

                            if (file.status === "error") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-rose-950/40 rounded-lg flex flex-col gap-2.5 text-xs">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-rose-400 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "失敗"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-455 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-rose-500/20 bg-rose-550/5 rounded-lg text-rose-400 text-[11px] font-sans flex items-start gap-2">
                                    <AlertTriangle className="w-4 h-4 text-rose-400 shrink-0 mt-0.5" />
                                    <div className="flex-1 flex flex-col gap-1">
                                      <span className="font-bold">❌ 解析失敗：</span>
                                      <span className="font-mono text-[10px] text-rose-300 leading-normal break-all">{file.error || "未知錯誤"}</span>
                                    </div>
                                  </div>
                                </div>
                              );
                            }

                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-955 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 自動學術分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-455 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">分類變更:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        if (origIdx !== -1) {
                                          const newArr = [...batchTexts];
                                          newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                          setBatchTexts(newArr);
                                        }
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(file.content);
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            if (origIdx !== -1) {
                                              const newArr = [...batchTexts];
                                              newArr[origIdx].content = autoCorrectText(file.content);
                                              setBatchTexts(newArr);
                                            }
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-955 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-450 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-400 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                      <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                        提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                      </p>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={file.content}
                                  onChange={(e) => {
                                    if (origIdx !== -1) {
                                      const newArr = [...batchTexts];
                                      newArr[origIdx].content = e.target.value;
                                      setBatchTexts(newArr);
                                    }
                                  }}
                                  rows={3}
                                  className="w-full bg-slate-900 border border-slate-855 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-mono text-[11px]"
                                />
                              </div>
                            );
                          })
                        )
                      ) : (
                        (() => {
                          const filtered = vaultItems.filter(item => {
                            if (ingestFilter !== "all" && (item.category || classifyFileCategory(item.source)) !== ingestFilter) return false;
                            const q = vaultSearchQuery.toLowerCase().trim();
                            if (!q) return true;
                            return (
                              (item.source && item.source.toLowerCase().includes(q)) ||
                              (item.text && item.text.toLowerCase().includes(q)) ||
                              (item.category && item.category.toLowerCase().includes(q))
                            );
                          });

                          if (filtered.length === 0) {
                            return (
                              <div className="text-center py-8 bg-slate-955/40 border border-slate-850 rounded-lg">
                                <p className="text-xs text-slate-500 italic">此篩選與搜尋條件下無任何智商庫項目。</p>
                              </div>
                            );
                          }

                          return filtered.map((item, idx) => {
                            const origVIdx = vaultItems.findIndex(v => v.id === item.id);
                            const activeCat = item.category || classifyFileCategory(item.source);

                            return (
                              <div key={item.id || idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs relative overflow-hidden group">
                                <div className="absolute top-0 left-0 w-full h-[2.5px] bg-gradient-to-r from-amber-600 to-amber-700 opacity-60 group-hover:opacity-100 transition-opacity" />
                                
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-550 font-semibold truncate max-w-[280px]" title={item.source}>📄 {item.source}</span>
                                  <span className="text-slate-500 shrink-0">{(item.text ? item.text.length : 0)} 字 | UTF-8</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 智商庫分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-455 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">變更分類:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        if (origVIdx !== -1) {
                                          const newArr = [...vaultItems];
                                          newArr[origVIdx] = { ...newArr[origVIdx], category: e.target.value as any };
                                          setVaultItems(newArr);
                                        }
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(item.text || "");
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            if (origVIdx !== -1) {
                                              const newArr = [...vaultItems];
                                              newArr[origVIdx].text = autoCorrectText(item.text || "");
                                              setVaultItems(newArr);
                                            }
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-955 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-955 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-450 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-400 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={item.text || ""}
                                  onChange={(e) => {
                                    if (origVIdx !== -1) {
                                      const newArr = [...vaultItems];
                                      newArr[origVIdx].text = e.target.value;
                                      setVaultItems(newArr);
                                    }
                                  }}
                                  rows={4}
                                  className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-mono text-[11px]"
                                />

                                {/* Card Actions */}
                                <div className="flex justify-between items-center border-t border-slate-850/60 pt-2.5 mt-1">
                                  <button
                                    type="button"
                                    onClick={() => deleteVaultItem(item.id)}
                                    className="px-2.5 py-1.5 bg-rose-500/10 hover:bg-rose-500 border border-rose-500/25 text-rose-450 hover:text-white transition-all text-[11px] font-semibold rounded-md flex items-center gap-1 cursor-pointer"
                                  >
                                    <Trash2 className="w-3.5 h-3.5" />
                                    <span>永久刪除</span>
                                  </button>

                                  <div className="flex gap-1.5">
                                    <button
                                      type="button"
                                      onClick={() => {
                                        navigator.clipboard.writeText(item.text || "");
                                        showAlert("✅ 已成功複製教材全文！");
                                      }}
                                      className="px-2.5 py-1.5 bg-slate-900 hover:bg-slate-850 border border-slate-800 text-slate-350 hover:text-slate-200 text-[11px] font-semibold rounded-md transition-colors cursor-pointer"
                                    >
                                      複製全文
                                    </button>
                                    <button
                                      type="button"
                                      onClick={() => updateVaultItem(item.id, item.text || "", item.category)}
                                      className="px-2.5 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 text-[11px] font-bold rounded-md transition-all cursor-pointer"
                                    >
                                      💾 儲存修改
                                    </button>
                                  </div>
                                </div>
                              </div>
                            );
                          });
                        })()
                      )}
                    </div>

                    {queueTab === "pending" && (
                      <button 
                        onClick={handleBatchIngest}
                        disabled={isIngesting}
                        className="w-full py-3 bg-gradient-to-r from-amber-600 to-amber-700 text-white rounded-lg font-semibold hover:brightness-110 active:brightness-95 transition-all cursor-pointer text-xs flex items-center justify-center gap-2"
                      >
                        {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫"}
                      </button>
                    )}

--------------------------------------------------------------------------------

=== Line 1362 | Step 1190 | Tool replace_file_content ===
Description: Restore card map loop and implement pending/vault cards conditional rendering in App.tsx.
Instruction: Replace lines 3568 to 3722 with the clean, complete JSX rendering logic for both Tab views.
TargetContent:
                    <div className="space-y-3.5 max-h-[480px] overflow-y-auto pr-1">
                      {queueTab === "pending" ? (
                        batchTexts.filter(file => {
                          if (ingestFilter === "all") return true;
                          const fileCat = file.category || classifyFileCategory(file.name);
                          return fileCat === ingestFilter;
                            );
                          }

                          if (file.status === "error") {
                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-955 border border-rose-955/40 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-rose-400 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">
                                    {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "失敗"} | UTF-8
                                  </span>
                                </div>
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 預定分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                </div>
                                <div className="p-2 border border-rose-500/20 bg-rose-550/5 rounded-lg text-rose-400 text-[11px] font-sans flex items-start gap-2">
                                  <AlertTriangle className="w-4 h-4 text-rose-400 shrink-0 mt-0.5" />
                                  <div className="flex-1 flex flex-col gap-1">
                                    <span className="font-bold">❌ 解析失敗：</span>
                                    <span className="font-mono text-[10px] text-rose-300 leading-normal break-all">{file.error || "未知錯誤"}</span>
                                  </div>
                                </div>
                              </div>
                            );
                          }

                          return (
                            <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs">
                              <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                              </div>

                              {/* Classification Field */}
                              <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                  <span>🏫 自動學術分類:</span>
                                  <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                    activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                    activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                    activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                    activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-550/20' :
                                    'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                  }`}>
                                    {activeCat}
                                  </span>
                                </span>
                                <div className="flex items-center gap-1.5">
                                  <span className="text-slate-500 font-sans">分類變更:</span>
                                  <select
                                    value={activeCat}
                                    onChange={(e) => {
                                      if (origIdx !== -1) {
                                        const newArr = [...batchTexts];
                                        newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                        setBatchTexts(newArr);
                                      }
                                    }}
                                    className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                  >
                                    <option value="判例">⚖️ 判例</option>
                                    <option value="證物">📁 證物</option>
                                    <option value="學術錄影">🎥 學術錄影</option>
                                    <option value="學術錄音">📻 學術錄音</option>
                                    <option value="學術教材">📖 學術教材</option>
                                  </select>
                                </div>
                              </div>

                              {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                              {(() => {
                                const detected = proofreadText(file.content);
                                if (detected.length === 0) return null;
                                return (
                                  <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                    <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                      <span className="flex items-center gap-1">
                                        <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                        ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                      </span>
                                      <button
                                        type="button"
                                        onClick={() => {
                                          if (origIdx !== -1) {
                                            const newArr = [...batchTexts];
                                            newArr[origIdx].content = autoCorrectText(file.content);
                                            setBatchTexts(newArr);
                                          }
                                        }}
                                        className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                      >
                                        <span>⚡ 一鍵本卡校正</span>
                                      </button>
                                    </div>
                                    <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5">
                                      {detected.map((match, mIdx) => (
                                        <span 
                                          key={match.id + '-' + mIdx} 
                                          className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                          title={`${match.reason} (建議修正為：${match.correct})`}
                                        >
                                          <span className="text-rose-450 line-through mr-1">{match.suspect}</span>
                                          <span className="text-slate-600">➔</span>
                                          <span className="text-emerald-400 font-bold ml-1">{match.correct}</span>
                                        </span>
                                      ))}
                                    </div>
                                    <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                      提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                    </p>
                                  </div>
                                );
                              })()}

                              <textarea 
                                value={file.content}
                                onChange={(e) => {
                                  if (origIdx !== -1) {
                                    const newArr = [...batchTexts];
                                    newArr[origIdx].content = e.target.value;
                                    setBatchTexts(newArr);
                                  }
                                }}
                                rows={3}
                                className="w-full bg-slate-900 border border-slate-855 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-mono text-[11px]"
                              />
                            </div>
                          );
                        })
                      )}
                    </div>

                    <button 
                      onClick={handleBatchIngest}
                      disabled={isIngesting}
                      className="w-full py-3 bg-gradient-to-r from-amber-600 to-amber-700 text-white rounded-lg font-semibold hover:brightness-110 active:brightness-95 transition-all cursor-pointer text-xs flex items-center justify-center gap-2"
                    >
                      {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫"}
                    </button>

ReplacementContent:
                    <div className="space-y-3.5 max-h-[480px] overflow-y-auto pr-1">
                      {queueTab === "pending" ? (
                        batchTexts.filter(file => {
                          if (ingestFilter === "all") return true;
                          const fileCat = file.category || classifyFileCategory(file.name);
                          return fileCat === ingestFilter;
                        }).length === 0 ? (
                          <div className="text-center py-8 bg-slate-950/40 border border-slate-850 rounded-lg">
                            <p className="text-xs text-slate-500 italic">此分類篩選下無任何檔案項目。</p>
                          </div>
                        ) : (
                          batchTexts.filter(file => {
                            if (ingestFilter === "all") return true;
                            const fileCat = file.category || classifyFileCategory(file.name);
                            return fileCat === ingestFilter;
                          }).map((file, idx) => {
                            const origIdx = batchTexts.findIndex(b => b.name === file.name);
                            const activeCat = file.category || classifyFileCategory(file.name);

                            if (file.status === "pending") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950/60 border border-dashed border-slate-850 rounded-lg flex flex-col gap-2.5 text-xs">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-slate-450 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "等待中"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-455 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-slate-850/60 bg-slate-900/30 rounded-lg text-slate-450 text-[11px] font-sans flex items-center gap-2">
                                    <Clock className="w-3.5 h-3.5 text-slate-500 animate-pulse" />
                                    <span>⏳ 排程中，等待 GPU ASR 語音轉譯/OCR 解析...</span>
                                  </div>
                                </div>
                              );
                            }

                            if (file.status === "parsing") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950/80 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs ring-1 ring-cyan-500/10">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-cyan-455 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "解析中"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-450 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-cyan-500/20 bg-cyan-500/5 rounded-lg text-cyan-400 text-[11px] font-sans flex items-center gap-2">
                                    <RefreshCw className="w-3.5 h-3.5 text-cyan-400 animate-spin" />
                                    <span className="font-medium animate-pulse">🔄 正在進行本機 GPU/FFmpeg 智慧提取中...</span>
                                  </div>
                                </div>
                              );
                            }

                            if (file.status === "error") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-rose-950/40 rounded-lg flex flex-col gap-2.5 text-xs">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-rose-400 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "失敗"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-rose-500/20 bg-rose-550/5 rounded-lg text-rose-400 text-[11px] font-sans flex items-start gap-2">
                                    <AlertTriangle className="w-4 h-4 text-rose-400 shrink-0 mt-0.5" />
                                    <div className="flex-1 flex flex-col gap-1">
                                      <span className="font-bold">❌ 解析失敗：</span>
                                      <span className="font-mono text-[10px] text-rose-300 leading-normal break-all">{file.error || "未知錯誤"}</span>
                                    </div>
                                  </div>
                                </div>
                              );
                            }

                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 自動學術分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">分類變更:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        if (origIdx !== -1) {
                                          const newArr = [...batchTexts];
                                          newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                          setBatchTexts(newArr);
                                        }
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(file.content);
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            if (origIdx !== -1) {
                                              const newArr = [...batchTexts];
                                              newArr[origIdx].content = autoCorrectText(file.content);
                                              setBatchTexts(newArr);
                                            }
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-955 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-450 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-400 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                      <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                        提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                      </p>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={file.content}
                                  onChange={(e) => {
                                    if (origIdx !== -1) {
                                      const newArr = [...batchTexts];
                                      newArr[origIdx].content = e.target.value;
                                      setBatchTexts(newArr);
                                    }
                                  }}
                                  rows={3}
                                  className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-mono text-[11px]"
                                />
                              </div>
                            );
                          })
                        )
                      ) : (
                        (() => {
                          const filtered = vaultItems.filter(item => {
                            if (ingestFilter !== "all" && (item.category || classifyFileCategory(item.source)) !== ingestFilter) return false;
                            const q = vaultSearchQuery.toLowerCase().trim();
                            if (!q) return true;
                            return (
                              (item.source && item.source.toLowerCase().includes(q)) ||
                              (item.text && item.text.toLowerCase().includes(q)) ||
                              (item.category && item.category.toLowerCase().includes(q))
                            );
                          });

                          if (filtered.length === 0) {
                            return (
                              <div className="text-center py-8 bg-slate-950/40 border border-slate-850 rounded-lg">
                                <p className="text-xs text-slate-500 italic">此篩選與搜尋條件下無任何智商庫項目。</p>
                              </div>
                            );
                          }

                          return filtered.map((item, idx) => {
                            const origVIdx = vaultItems.findIndex(v => v.id === item.id);
                            const activeCat = item.category || classifyFileCategory(item.source);

                            return (
                              <div key={item.id || idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs relative overflow-hidden group">
                                <div className="absolute top-0 left-0 w-full h-[2.5px] bg-gradient-to-r from-amber-600 to-amber-700 opacity-60 group-hover:opacity-100 transition-opacity" />
                                
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-505 font-semibold truncate max-w-[280px]" title={item.source}>📄 {item.source}</span>
                                  <span className="text-slate-500 shrink-0">{(item.text ? item.text.length : 0)} 字 | UTF-8</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-8-0 bg-transparent">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 智商庫分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">變更分類:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        if (origVIdx !== -1) {
                                          const newArr = [...vaultItems];
                                          newArr[origVIdx] = { ...newArr[origVIdx], category: e.target.value as any };
                                          setVaultItems(newArr);
                                        }
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(item.text || "");
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            if (origVIdx !== -1) {
                                              const newArr = [...vaultItems];
                                              newArr[origVIdx].text = autoCorrectText(item.text || "");
                                              setVaultItems(newArr);
                                            }
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-450 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-400 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={item.text || ""}
                                  onChange={(e) => {
                                    if (origVIdx !== -1) {
                                      const newArr = [...vaultItems];
                                      newArr[origVIdx].text = e.target.value;
                                      setVaultItems(newArr);
                                    }
                                  }}
                                  rows={4}
                                  className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-mono text-[11px]"
                                />

                                {/* Card Actions */}
                                <div className="flex justify-between items-center border-t border-slate-850/60 pt-2.5 mt-1">
                                  <button
                                    type="button"
                                    onClick={() => deleteVaultItem(item.id)}
                                    className="px-2.5 py-1 bg-rose-500/10 hover:bg-rose-500 border border-rose-500/25 text-rose-450 hover:text-white transition-all text-[11px] font-semibold rounded flex items-center gap-1 cursor-pointer"
                                  >
                                    <Trash2 className="w-3.5 h-3.5" />
                                    <span>永久刪除</span>
                                  </button>

                                  <div className="flex gap-1.5">
                                    <button
                                      type="button"
                                      onClick={() => {
                                        navigator.clipboard.writeText(item.text || "");
                                        showAlert("✅ 已成功複製教材全文！");
                                      }}
                                      className="px-2 py-1 bg-slate-900 hover:bg-slate-850 border border-slate-800 text-slate-350 hover:text-slate-200 text-[11px] font-semibold rounded transition-colors cursor-pointer"
                                    >
                                      複製
                                    </button>
                                    <button
                                      type="button"
                                      onClick={() => updateVaultItem(item.id, item.text || "", item.category)}
                                      className="px-2.5 py-1 bg-amber-500 hover:bg-amber-600 text-slate-950 text-[11px] font-bold rounded transition-all cursor-pointer"
                                    >
                                      儲存修改
                                    </button>
                                  </div>
                                </div>
                              </div>
                            );
                          });
                        })()
                      )}
                    </div>

                    {queueTab === "pending" && (
                      <button 
                        onClick={handleBatchIngest}
                        disabled={isIngesting}
                        className="w-full py-3 bg-gradient-to-r from-amber-600 to-amber-700 text-white rounded-lg font-semibold hover:brightness-110 active:brightness-95 transition-all cursor-pointer text-xs flex items-center justify-center gap-2"
                      >
                        {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫"}
                      </button>
                    )}

--------------------------------------------------------------------------------

=== Line 1433 | Step 1262 | Tool replace_file_content ===
Description: Reconstructed and fixed the corrupted Tab 2 JSX block in App.tsx. Integrated persistent database/vault explorer directly into the queue panel with tab toggle, inline editing, category change, proofreading, correction, saving, and deletion.
Instruction: Replace the corrupted region in App.tsx from line 3367 to line 3611 with the clean integrated code for Tab 2.
TargetContent:
                    {/* Succeeded Result Preview Widget and Injection Action */}
                    {lastScrapedResult && (
                      <motion.div
                        initial={{ opacity: 0, y: 8 }}
                        animate={{ opacity: 1, y: 0 }}
                        className="p-3 border border-slate-800 bg-slate-950/60 rounded-xl mt-1 flex flex-col gap-2 relative"
                      >
                        <div className="flex justify-between items-start gap-3">
                          <div className="flex-1">
                            <span className="text-[9px] font-bold text-slate-500 font-mono tracking-wider uppercase bg-slate-900 border border-slate-800 px-1.5 py-0.5 rounded">
                              ✨ 成功抓取預覽 | {lastScrapedResult.category}
                            </span>
                            <h4 className="text-[12px] font-bold text-white mt-1.5 font-display leading-tight">{lastScrapedResult.title}</h4>
                          </div>
                          <span className="text-[10px] text-slate-500 font-mono shrink-0">
                            ⚖️ 判例(
                            {
                              vaultItems.filter(
                                (b) =>
                                  (b.category ||
                                    classifyFileCategory(b.source)) === "判例"
                              ).length
                            }
                            ) | 📁 證物(
                            {
                              vaultItems.filter(
                                (b) =>
                                  (b.category ||
                                    classifyFileCategory(b.source)) === "證物"
                              ).length
                            }
                            ) | 🎥 影音(
                            {
                              vaultItems.filter((b) =>
                                ["學術錄影", "學術錄音"].includes(
                                  b.category ||
                                    classifyFileCategory(b.source) ||
                                    ""
                                )
                              ).length
                            }
                            )
                          </>
                        )}
                      </span>
                    </div>
                    <div className="flex flex-wrap gap-1.5 mt-1">
                      {[
                        { id: "all", label: "全部顯示", emoji: "🗂️" },
                        { id: "判例", label: "⚖️ 判例", emoji: "" },
                        { id: "證物", label: "📁 證物", emoji: "" },
                        { id: "學術錄影", label: "🎥 學術錄影", emoji: "" },
                        { id: "學術錄音", label: "📻 學術錄音", emoji: "" },
                        { id: "學術教材", label: "📖 學術教材", emoji: "" },
                      ].map((btn) => {
                        const count =
                          btn.id === "all"
                            ? queueTab === "pending"
                              ? batchTexts.length
                              : vaultItems.length
                            : queueTab === "pending"
                            ? batchTexts.filter(
                                (b) =>
                                  (b.category || classifyFileCategory(b.name)) ===
                                  btn.id
                              ).length
                            : vaultItems.filter(
                                (b) =>
                                  (b.category ||
                                    classifyFileCategory(b.source)) === btn.id
                              ).length;
                        const isActive = ingestFilter === btn.id;
                        return (
                          <button
                            key={btn.id}
                            type="button"
                            onClick={() => setIngestFilter(btn.id)}
                            className={`px-2.5 py-1 text-[11px] font-semibold rounded-md border transition-all cursor-pointer flex items-center gap-1 ${
                              isActive
                                ? "bg-amber-600/20 text-amber-455 border-amber-500/50 shadow-sm"
                                : "bg-slate-900 text-slate-400 border-slate-800 hover:text-slate-200 hover:border-slate-700"
                            }`}
                          >
                            <span>
                              {btn.emoji} {btn.label}
                            </span>
                            <span
                              className={`text-[10px] font-mono px-1 rounded-sm ${
                                isActive
                                  ? "bg-amber-400/20 text-amber-300"
                                  : "bg-slate-950 text-slate-500"
                              }`}
                            >
                            );
                          }

                          if (file.status === "error") {
                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-rose-950/40 rounded-lg flex flex-col gap-2.5 text-xs">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-rose-400 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">
                                    {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "失敗"} | UTF-8
                                  </span>
                                </div>
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 預定分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                </div>
                                <div className="p-2 border border-rose-500/20 bg-rose-550/5 rounded-lg text-rose-400 text-[11px] font-sans flex items-start gap-2">
                                  <AlertTriangle className="w-4 h-4 text-rose-400 shrink-0 mt-0.5" />
                                  <div className="flex-1 flex flex-col gap-1">
                                    <span className="font-bold">❌ 解析失敗：</span>
                                    <span className="font-mono text-[10px] text-rose-300 leading-normal break-all">{file.error || "未知錯誤"}</span>
                                  </div>
                                </div>
                              </div>
                            );
                          }

                          return (
                            <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs">
                              <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                              </div>

                              {/* Classification Field */}
                              <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                  <span>🏫 自動學術分類:</span>
                                  <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                    activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                    activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                    activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                    activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                    'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                  }`}>
                                    {activeCat}
                                  </span>
                                </span>
                                <div className="flex items-center gap-1.5">
                                  <span className="text-slate-500 font-sans">分類變更:</span>
                                  <select
                                    value={activeCat}
                                    onChange={(e) => {
                                      if (origIdx !== -1) {
                                        const newArr = [...batchTexts];
                                        newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                        setBatchTexts(newArr);
                                      }
                                    }}
                                    className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                  >
                                    <option value="判例">⚖️ 判例</option>
                                    <option value="證物">📁 證物</option>
                                    <option value="學術錄影">🎥 學術錄影</option>
                                    <option value="學術錄音">📻 學術錄音</option>
                                    <option value="學術教材">📖 學術教材</option>
                                  </select>
                                </div>
                              </div>

                              {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                              {(() => {
                                const detected = proofreadText(file.content);
                                if (detected.length === 0) return null;
                                return (
                                  <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                    <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                      <span className="flex items-center gap-1">
                                        <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                        ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                      </span>
                                      <button
                                        type="button"
                                        onClick={() => {
                                          if (origIdx !== -1) {
                                            const newArr = [...batchTexts];
                                            newArr[origIdx].content = autoCorrectText(file.content);
                                            setBatchTexts(newArr);
                                          }
                                        }}
                                        className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                      >
                                        <span>⚡ 一鍵本卡校正</span>
                                      </button>
                                    </div>
                                    <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5 font-sans">
                                      {detected.map((match, mIdx) => (
                                        <span 
                                          key={match.id + '-' + mIdx} 
                                          className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                          title={`${match.reason} (建議修正為：${match.correct})`}
                                        >
                                          <span className="text-rose-455 line-through mr-1">{match.suspect}</span>
                                          <span className="text-slate-600">➔</span>
                                          <span className="text-emerald-450 font-bold ml-1">{match.correct}</span>
                                        </span>
                                      ))}
                                    </div>
                                    <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                      提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                    </p>
                                  </div>
                                );
                              })()}

                              <textarea 
                                value={file.content}
                                onChange={(e) => {
                                  if (origIdx !== -1) {
                                    const newArr = [...batchTexts];
                                    newArr[origIdx].content = e.target.value;
                                    setBatchTexts(newArr);
                                  }
                                }}
                                rows={3}
                                className="w-full bg-slate-900 border border-slate-855 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-sans"
                              />
                            </div>
                          );
                        })
                      )}
                    </div>

                    <button 
                      onClick={handleBatchIngest}
                      disabled={isIngesting}
                      className="w-full py-3 bg-gradient-to-r from-amber-600 to-amber-700 text-white rounded-lg font-semibold hover:brightness-110 active:brightness-95 transition-all cursor-pointer text-xs flex items-center justify-center gap-2"
                    >
                      {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫"}
                    </button>
                  </div>
                </div>

ReplacementContent:
                    {/* Succeeded Result Preview Widget and Injection Action */}
                    {lastScrapedResult && (
                      <motion.div
                        initial={{ opacity: 0, y: 8 }}
                        animate={{ opacity: 1, y: 0 }}
                        className="p-3 border border-slate-800 bg-slate-950/60 rounded-xl mt-1 flex flex-col gap-2 relative"
                      >
                        <div className="flex justify-between items-start gap-3">
                          <div className="flex-1">
                            <span className="text-[9px] font-bold text-slate-500 font-mono tracking-wider uppercase bg-slate-900 border border-slate-800 px-1.5 py-0.5 rounded">
                              ✨ 成功抓取預覽 | {lastScrapedResult.category}
                            </span>
                            <h4 className="text-[12px] font-bold text-white mt-1.5 font-display leading-tight">{lastScrapedResult.title}</h4>
                          </div>
                          <span className="text-[10px] text-slate-500 font-mono shrink-0">
                            {Math.round(lastScrapedResult.content.length)} 字
                          </span>
                        </div>
                        
                        <div className="text-[10px] text-slate-400 font-sans border-t border-slate-850/60 pt-2 line-clamp-3 leading-relaxed whitespace-pre-wrap">
                          {lastScrapedResult.content}
                        </div>

                        <div className="flex gap-2 mt-1">
                          <button
                            type="button"
                            onClick={handleEnqueueScrapedResult}
                            className="flex-1 py-1.5 bg-cyan-500/15 border border-cyan-500/35 hover:bg-cyan-500/25 active:bg-cyan-500/35 text-cyan-400 font-bold text-[11px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer"
                          >
                            <span>📥 自動匯入下方批次教材隊列</span>
                          </button>
                          <button
                            type="button"
                            onClick={() => setLastScrapedResult(null)}
                            className="px-3 py-1.5 bg-slate-900 hover:bg-slate-850 border border-slate-800 hover:border-slate-700 text-slate-400 hover:text-slate-350 text-[11px] rounded-lg transition-all cursor-pointer"
                          >
                            捨棄
                          </button>
                        </div>
                      </motion.div>
                    )}
                  </div>

                  <div className="p-4 bg-slate-900/80 border border-slate-800 rounded-xl flex flex-col gap-3">
                    <div className="flex justify-between items-center border-b border-slate-850 pb-2 mb-1">
                      <div className="flex gap-3">
                        <button
                          type="button"
                          onClick={() => setQueueTab("pending")}
                          className={`text-xs font-bold pb-1.5 transition-all cursor-pointer border-b-2 px-1 ${
                            queueTab === "pending"
                              ? "text-amber-500 border-amber-500"
                              : "text-slate-400 border-transparent hover:text-slate-200"
                          }`}
                        >
                          ⏳ 待校對隊列 ({batchTexts.length})
                        </button>
                        <button
                          type="button"
                          onClick={() => setQueueTab("vault")}
                          className={`text-xs font-bold pb-1.5 transition-all cursor-pointer border-b-2 px-1 ${
                            queueTab === "vault"
                              ? "text-amber-500 border-amber-500"
                              : "text-slate-400 border-transparent hover:text-slate-200"
                          }`}
                        >
                          📚 已入庫智商庫 ({vaultItems.length})
                        </button>
                      </div>
                      
                      {queueTab === "pending" ? (
                        batchTexts.length > 0 && (
                          <button 
                            type="button"
                            onClick={() => {
                              if (confirm("確定要清空材料隊列嗎？")) {
                                setBatchTexts([]);
                              }
                            }}
                            className="text-[10px] text-rose-450 hover:text-rose-350 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent"
                          >
                            <Trash2 className="w-3.5 h-3.5" /> 清空隊列
                          </button>
                        )
                      ) : (
                        <button
                          type="button"
                          onClick={fetchVaultItems}
                          disabled={isVaultLoading}
                          className="text-[10px] text-slate-400 hover:text-slate-200 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent"
                        >
                          <RefreshCw className={`w-3.5 h-3.5 ${isVaultLoading ? "animate-spin" : ""}`} /> 重新整理
                        </button>
                      )}
                    </div>

                    {/* Global auto-correct rule engine action */}
                    {queueTab === "pending" && batchTexts.length > 0 && (
                      <div className="bg-amber-500/10 border border-amber-500/15 p-3 rounded-lg flex flex-col sm:flex-row items-center justify-between gap-3 text-xs">
                        <div className="flex items-center gap-2">
                          <span className="p-1 px-1.5 bg-amber-500/20 text-amber-400 font-bold rounded text-[9px] border border-amber-500/10 shrink-0 font-mono">
                            OFFLINE ENGINE
                          </span>
                          <p className="text-[11px] text-slate-300 font-sans leading-relaxed">
                            <b>LanguageTool 離線法律校對核心：</b>已自動在背景套用繁體法律術語和語音 (ASR) 混淆音判定規則。
                          </p>
                        </div>
                        <button
                          type="button"
                          onClick={() => {
                            const updated = batchTexts.map(f => ({
                              ...f,
                              content: autoCorrectText(f.content)
                            }));
                            setBatchTexts(updated);
                          }}
                          className="w-full sm:w-auto px-3 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 rounded-lg font-bold text-[11px] transition-all flex items-center justify-center gap-1 shrink-0 border-none cursor-pointer"
                        >
                          <CheckCircle className="w-3.5 h-3.5 text-slate-950" />
                          一鍵校對修正全體隊列
                        </button>
                      </div>
                    )}

                    {/* Category Filter selector */}
                    <div className="bg-slate-950 border border-slate-850 rounded-lg p-2.5 flex flex-col gap-1.5">
                      <div className="text-[11px] text-slate-400 font-semibold tracking-wider flex items-center justify-between font-sans">
                        <span className="flex items-center gap-1 text-slate-300">🔍 教材標籤篩選器</span>
                        <span className="text-[10px] text-slate-500 font-mono">
                          ⚖️ 判例(
                          {
                            queueTab === "pending"
                              ? batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === '判例').length
                              : vaultItems.filter(b => b.category === '判例').length
                          }
                          ) | 📁 證物(
                          {
                            queueTab === "pending"
                              ? batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === '證物').length
                              : vaultItems.filter(b => b.category === '證物').length
                          }
                          ) | 🎥 影音(
                          {
                            queueTab === "pending"
                              ? batchTexts.filter(b => ['學術錄影', '學術錄音'].includes(b.category || classifyFileCategory(b.name) || '')).length
                              : vaultItems.filter(b => ['學術錄影', '學術錄音'].includes(b.category || '')).length
                          }
                          )
                        </span>
                      </div>
                      <div className="flex flex-wrap gap-1.5 mt-1">
                        {[
                          { id: "all", label: "全部顯示", emoji: "🗂️" },
                          { id: "判例", label: "⚖️ 判例", emoji: "" },
                          { id: "證物", label: "📁 證物", emoji: "" },
                          { id: "學術錄影", label: "🎥 學術錄影", emoji: "" },
                          { id: "學術錄音", label: "📻 學術錄音", emoji: "" },
                          { id: "學術教材", label: "📖 學術教材", emoji: "" }
                        ].map((btn) => {
                          const count = btn.id === "all"
                            ? (queueTab === "pending" ? batchTexts.length : vaultItems.length)
                            : (queueTab === "pending"
                                ? batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === btn.id).length
                                : vaultItems.filter(b => b.category === btn.id).length);
                          const isActive = ingestFilter === btn.id;
                          return (
                            <button
                              key={btn.id}
                              type="button"
                              onClick={() => setIngestFilter(btn.id)}
                              className={`px-2.5 py-1 text-[11px] font-semibold rounded-md border transition-all cursor-pointer flex items-center gap-1 ${
                                isActive 
                                  ? "bg-amber-600/20 text-amber-450 border-amber-500/50 shadow-sm"
                                  : "bg-slate-900 text-slate-400 border-slate-800 hover:text-slate-200 hover:border-slate-700"
                              }`}
                            >
                              <span>{btn.emoji} {btn.label}</span>
                              <span className={`text-[10px] font-mono px-1 rounded-sm ${isActive ? "bg-amber-400/20 text-amber-300" : "bg-slate-950 text-slate-500"}`}>{count}</span>
                            </button>
                          );
                        })}
                      </div>
                    </div>

                    {/* Integrated Search Input for Vault */}
                    {queueTab === "vault" && (
                      <div className="relative">
                        <input
                          type="text"
                          value={vaultSearchQuery}
                          onChange={(e) => setVaultSearchQuery(e.target.value)}
                          placeholder="🔍 搜尋已存入之教材名稱或逐字稿關鍵字（如：民法、上課錄影、判例）..."
                          className="w-full bg-slate-950 border border-slate-850 focus:border-amber-500/80 rounded-lg py-2 pl-3 pr-8 text-xs text-slate-350 focus:outline-none transition-all font-sans"
                        />
                        {vaultSearchQuery && (
                          <button
                            type="button"
                            onClick={() => setVaultSearchQuery("")}
                            className="absolute right-2.5 top-2.5 text-slate-500 hover:text-slate-300 bg-transparent border-0 cursor-pointer"
                          >
                            <X className="w-3.5 h-3.5" />
                          </button>
                        )}
                      </div>
                    )}

                    <div className="space-y-3.5 max-h-[480px] overflow-y-auto pr-1">
                      {queueTab === "pending" ? (
                        batchTexts.filter(file => {
                          if (ingestFilter === "all") return true;
                          const fileCat = file.category || classifyFileCategory(file.name);
                          return fileCat === ingestFilter;
                        }).length === 0 ? (
                          <div className="text-center py-8 bg-slate-950/40 border border-slate-850 rounded-lg">
                            <p className="text-xs text-slate-500 italic font-sans">此分類篩選下無任何檔案項目。</p>
                          </div>
                        ) : (
                          batchTexts.filter(file => {
                            if (ingestFilter === "all") return true;
                            const fileCat = file.category || classifyFileCategory(file.name);
                            return fileCat === ingestFilter;
                          }).map((file, idx) => {
                            const origIdx = batchTexts.findIndex(b => b.name === file.name);
                            const activeCat = file.category || classifyFileCategory(file.name);
                            
                            if (file.status === "error") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-rose-950/40 rounded-lg flex flex-col gap-2.5 text-xs animate-[fadeIn_0.2s_ease-out]">
                                  <div className="flex justify-between items-center border-b border-slate-855 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-rose-400 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "失敗"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-rose-500/20 bg-rose-550/5 rounded-lg text-rose-400 text-[11px] font-sans flex items-start gap-2">
                                    <AlertTriangle className="w-4 h-4 text-rose-400 shrink-0 mt-0.5" />
                                    <div className="flex-1 flex flex-col gap-1">
                                      <span className="font-bold">❌ 解析失敗：</span>
                                      <span className="font-mono text-[10px] text-rose-300 leading-normal break-all">{file.error || "未知錯誤"}</span>
                                    </div>
                                  </div>
                                </div>
                              );
                            }

                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs animate-[fadeIn_0.2s_ease-out]">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 自動學術分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">分類變更:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        if (origIdx !== -1) {
                                          const newArr = [...batchTexts];
                                          newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                          setBatchTexts(newArr);
                                        }
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(file.content);
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            if (origIdx !== -1) {
                                              const newArr = [...batchTexts];
                                              newArr[origIdx].content = autoCorrectText(file.content);
                                              setBatchTexts(newArr);
                                            }
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5 font-sans">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-455 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-450 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                      <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                        提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                      </p>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={file.content}
                                  onChange={(e) => {
                                    if (origIdx !== -1) {
                                      const newArr = [...batchTexts];
                                      newArr[origIdx].content = e.target.value;
                                      setBatchTexts(newArr);
                                    }
                                  }}
                                  rows={3}
                                  className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-sans"
                                />
                              </div>
                            );
                          })
                        )
                      ) : (
                        (() => {
                          const filteredVaultItems = vaultItems.filter(item => {
                            // Category filter
                            if (ingestFilter !== "all") {
                              const fileCat = item.category || "學術教材";
                              if (fileCat !== ingestFilter) return false;
                            }
                            // Search filter
                            const q = vaultSearchQuery.toLowerCase().trim();
                            if (q) {
                              const matchesSource = item.source && item.source.toLowerCase().includes(q);
                              const matchesText = item.text && item.text.toLowerCase().includes(q);
                              const matchesCat = item.category && item.category.toLowerCase().includes(q);
                              if (!matchesSource && !matchesText && !matchesCat) return false;
                            }
                            return true;
                          });

                          if (filteredVaultItems.length === 0) {
                            return (
                              <div className="text-center py-8 bg-slate-950/40 border border-slate-850 border-dashed rounded-lg">
                                <p className="text-xs text-slate-500 italic font-sans">此篩選條件下智商庫無任何教材項目。</p>
                              </div>
                            );
                          }

                          return filteredVaultItems.map((item, idx) => {
                            const activeCat = item.category || "學術教材";
                            const fileExt = item.source.split('.').pop()?.toUpperCase() || "TXT";
                            
                            return (
                              <div key={item.id || idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs animate-[fadeIn_0.2s_ease-out]">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-500 font-semibold truncate max-w-[250px]" title={item.source}>📄 {item.source}</span>
                                  <span className="text-slate-500 shrink-0">{(item.text ? item.text.length : 0)} 字 | {fileExt}</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 目前智商分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">分類變更:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        const updated = vaultItems.map(vi => vi.id === item.id ? { ...vi, category: e.target.value } : vi);
                                        setVaultItems(updated);
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(item.text || "");
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            const updated = vaultItems.map(vi => vi.id === item.id ? { ...vi, text: autoCorrectText(vi.text) } : vi);
                                            setVaultItems(updated);
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5 font-sans">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-455 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-450 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={item.text || ""}
                                  onChange={(e) => {
                                    const updated = vaultItems.map(vi => vi.id === item.id ? { ...vi, text: e.target.value } : vi);
                                    setVaultItems(updated);
                                  }}
                                  rows={4}
                                  className="w-full bg-slate-900 border border-slate-855 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-sans"
                                />

                                {/* Save, Copy, Delete Inline Buttons */}
                                <div className="flex justify-between items-center gap-2 mt-1 pt-1.5 border-t border-slate-900/60">
                                  <button
                                    type="button"
                                    onClick={() => deleteVaultItem(item.id)}
                                    className="px-2 py-1 bg-rose-500/10 hover:bg-rose-500 hover:text-white border border-rose-500/20 text-rose-400 rounded text-[10px] transition-all cursor-pointer flex items-center gap-1 active:scale-95 border-none"
                                  >
                                    <Trash2 className="w-3.5 h-3.5" />
                                    <span>🗑️ 永久刪除</span>
                                  </button>
                                  <div className="flex gap-2">
                                    <button
                                      type="button"
                                      onClick={() => {
                                        navigator.clipboard.writeText(item.text || "");
                                        showAlert("📋 全文已成功複製到剪貼簿！");
                                      }}
                                      className="px-2 py-1 bg-slate-900 hover:bg-slate-800 border border-slate-800 text-slate-350 rounded text-[10px] transition-all cursor-pointer flex items-center gap-1 active:scale-95"
                                    >
                                      <span>複製全文</span>
                                    </button>
                                    <button
                                      type="button"
                                      onClick={() => updateVaultItem(item.id, item.text, item.category)}
                                      className="px-2.5 py-1 bg-emerald-600 hover:bg-emerald-500 text-white rounded text-[10px] font-bold transition-all cursor-pointer flex items-center gap-1 active:scale-95 border-none"
                                    >
                                      <span>💾 儲存修改</span>
                                    </button>
                                  </div>
                                </div>
                              </div>
                            );
                          });
                        })()
                      )}
                    </div>

                    {queueTab === "pending" && (
                      <button 
                        onClick={handleBatchIngest}
                        disabled={isIngesting}
                        className="w-full py-3 bg-gradient-to-r from-amber-600 to-amber-700 text-white rounded-lg font-semibold hover:brightness-110 active:brightness-95 transition-all cursor-pointer text-xs flex items-center justify-center gap-2 mt-2 border-none"
                      >
                        {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫"}
                      </button>
                    )}
                  </div>
                </div>

--------------------------------------------------------------------------------

=== Line 1439 | Step 1268 | Tool replace_file_content ===
Description: Replaced the corrupted JSX block (lines 3367 to 3528) with the fully integrated right-hand panel tab toggle (pending vs vault), category filter, search bar, list renderer, and inline editor.
Instruction: Replace the broken JSX block between lastScrapedResult and the end of column grid with the integrated tab panel.
TargetContent:
None

ReplacementContent:
                    {/* Succeeded Result Preview Widget and Injection Action */}
                    {lastScrapedResult && (
                      <motion.div
                        initial={{ opacity: 0, y: 8 }}
                        animate={{ opacity: 1, y: 0 }}
                        className="p-3 border border-slate-800 bg-slate-950/60 rounded-xl mt-1 flex flex-col gap-2 relative"
                      >
                        <div className="flex justify-between items-start gap-3">
                          <div className="flex-1">
                            <span className="text-[9px] font-bold text-slate-500 font-mono tracking-wider uppercase bg-slate-900 border border-slate-800 px-1.5 py-0.5 rounded">
                              ✨ 成功抓取預覽 | {lastScrapedResult.category}
                            </span>
                            <h4 className="text-[12px] font-bold text-white mt-1.5 font-display leading-tight">{lastScrapedResult.title}</h4>
                          </div>
                          <span className="text-[10px] text-slate-500 font-mono shrink-0">
                            {Math.round(lastScrapedResult.content.length)} 字
                          </span>
                        </div>
                        
                        <div className="text-[10px] text-slate-400 font-sans border-t border-slate-850/60 pt-2 line-clamp-3 leading-relaxed whitespace-pre-wrap">
                          {lastScrapedResult.content}
                        </div>

                        <div className="flex gap-2 mt-1">
                          <button
                            type="button"
                            onClick={handleEnqueueScrapedResult}
                            className="flex-1 py-1.5 bg-cyan-500/15 border border-cyan-500/35 hover:bg-cyan-500/25 active:bg-cyan-500/35 text-cyan-400 font-bold text-[11px] rounded-lg transition-all flex items-center justify-center gap-1 cursor-pointer"
                          >
                            <span>📥 自動匯入下方批次教材隊列</span>
                          </button>
                          <button
                            type="button"
                            onClick={() => setLastScrapedResult(null)}
                            className="px-3 py-1.5 bg-slate-900 hover:bg-slate-850 border border-slate-800 hover:border-slate-700 text-slate-400 hover:text-slate-355 text-[11px] rounded-lg transition-all cursor-pointer"
                          >
                            捨棄
                          </button>
                        </div>
                      </motion.div>
                    )}
                  </div>

                  <div className="p-4 bg-slate-900/80 border border-slate-800 rounded-xl flex flex-col gap-3">
                    <div className="flex justify-between items-center border-b border-slate-850 pb-2 mb-1">
                      <div className="flex gap-3">
                        <button
                          type="button"
                          onClick={() => setQueueTab("pending")}
                          className={`text-xs font-bold pb-1.5 transition-all cursor-pointer border-b-2 px-1 ${
                            queueTab === "pending"
                              ? "text-amber-500 border-amber-500"
                              : "text-slate-400 border-transparent hover:text-slate-200"
                          }`}
                        >
                          ⏳ 待校對隊列 ({batchTexts.length})
                        </button>
                        <button
                          type="button"
                          onClick={() => setQueueTab("vault")}
                          className={`text-xs font-bold pb-1.5 transition-all cursor-pointer border-b-2 px-1 ${
                            queueTab === "vault"
                              ? "text-amber-500 border-amber-500"
                              : "text-slate-400 border-transparent hover:text-slate-200"
                          }`}
                        >
                          📚 已入庫智商庫 ({vaultItems.length})
                        </button>
                      </div>
                      
                      {queueTab === "pending" ? (
                        batchTexts.length > 0 && (
                          <button 
                            type="button"
                            onClick={() => {
                              if (confirm("確定要清空材料隊列嗎？")) {
                                setBatchTexts([]);
                              }
                            }}
                            className="text-[10px] text-rose-450 hover:text-rose-350 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent"
                          >
                            <Trash2 className="w-3.5 h-3.5" /> 清空隊列
                          </button>
                        )
                      ) : (
                        <button
                          type="button"
                          onClick={fetchVaultItems}
                          disabled={isVaultLoading}
                          className="text-[10px] text-slate-400 hover:text-slate-200 flex items-center gap-1 cursor-pointer font-normal border-0 bg-transparent"
                        >
                          <RefreshCw className={`w-3.5 h-3.5 ${isVaultLoading ? "animate-spin" : ""}`} /> 重新整理
                        </button>
                      )}
                    </div>

                    {/* Global auto-correct rule engine action */}
                    {queueTab === "pending" && batchTexts.length > 0 && (
                      <div className="bg-amber-500/10 border border-amber-500/15 p-3 rounded-lg flex flex-col sm:flex-row items-center justify-between gap-3 text-xs">
                        <div className="flex items-center gap-2">
                          <span className="p-1 px-1.5 bg-amber-500/20 text-amber-400 font-bold rounded text-[9px] border border-amber-500/10 shrink-0 font-mono">
                            OFFLINE ENGINE
                          </span>
                          <p className="text-[11px] text-slate-300 font-sans leading-relaxed">
                            <b>LanguageTool 離線法律校對核心：</b>已自動在背景套用繁體法律術語和語音 (ASR) 混淆音判定規則。
                          </p>
                        </div>
                        <button
                          type="button"
                          onClick={() => {
                            const updated = batchTexts.map(f => ({
                              ...f,
                              content: autoCorrectText(f.content)
                            }));
                            setBatchTexts(updated);
                          }}
                          className="w-full sm:w-auto px-3 py-1.5 bg-amber-500 hover:bg-amber-600 text-slate-950 rounded-lg font-bold text-[11px] transition-all flex items-center justify-center gap-1 shrink-0 border-none cursor-pointer"
                        >
                          <CheckCircle className="w-3.5 h-3.5 text-slate-950" />
                          一鍵校對修正全體隊列
                        </button>
                      </div>
                    )}

                    {/* Category Filter selector */}
                    <div className="bg-slate-950 border border-slate-850 rounded-lg p-2.5 flex flex-col gap-1.5">
                      <div className="text-[11px] text-slate-400 font-semibold tracking-wider flex items-center justify-between font-sans">
                        <span className="flex items-center gap-1 text-slate-300">🔍 教材標籤篩選器</span>
                        <span className="text-[10px] text-slate-500 font-mono">
                          ⚖️ 判例(
                          {
                            queueTab === "pending"
                              ? batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === '判例').length
                              : vaultItems.filter(b => b.category === '判例').length
                          }
                          ) | 📁 證物(
                          {
                            queueTab === "pending"
                              ? batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === '證物').length
                              : vaultItems.filter(b => b.category === '證物').length
                          }
                          ) | 🎥 影音(
                          {
                            queueTab === "pending"
                              ? batchTexts.filter(b => ['學術錄影', '學術錄音'].includes(b.category || classifyFileCategory(b.name) || '')).length
                              : vaultItems.filter(b => ['學術錄影', '學術錄音'].includes(b.category || '')).length
                          }
                          )
                        </span>
                      </div>
                      <div className="flex flex-wrap gap-1.5 mt-1">
                        {[
                          { id: "all", label: "全部顯示", emoji: "🗂️" },
                          { id: "判例", label: "⚖️ 判例", emoji: "" },
                          { id: "證物", label: "📁 證物", emoji: "" },
                          { id: "學術錄影", label: "🎥 學術錄影", emoji: "" },
                          { id: "學術錄音", label: "📻 學術錄音", emoji: "" },
                          { id: "學術教材", label: "📖 學術教材", emoji: "" }
                        ].map((btn) => {
                          const count = btn.id === "all"
                            ? (queueTab === "pending" ? batchTexts.length : vaultItems.length)
                            : (queueTab === "pending"
                                ? batchTexts.filter(b => (b.category || classifyFileCategory(b.name)) === btn.id).length
                                : vaultItems.filter(b => b.category === btn.id).length);
                          const isActive = ingestFilter === btn.id;
                          return (
                            <button
                              key={btn.id}
                              type="button"
                              onClick={() => setIngestFilter(btn.id)}
                              className={`px-2.5 py-1 text-[11px] font-semibold rounded-md border transition-all cursor-pointer flex items-center gap-1 ${
                                isActive 
                                  ? "bg-amber-600/20 text-amber-450 border-amber-500/50 shadow-sm"
                                  : "bg-slate-900 text-slate-400 border-slate-800 hover:text-slate-200 hover:border-slate-700"
                              }`}
                            >
                              <span>{btn.emoji} {btn.label}</span>
                              <span className={`text-[10px] font-mono px-1 rounded-sm ${isActive ? "bg-amber-400/20 text-amber-300" : "bg-slate-950 text-slate-500"}`}>{count}</span>
                            </button>
                          );
                        })}
                      </div>
                    </div>

                    {/* Integrated Search Input for Vault */}
                    {queueTab === "vault" && (
                      <div className="relative">
                        <input
                          type="text"
                          value={vaultSearchQuery}
                          onChange={(e) => setVaultSearchQuery(e.target.value)}
                          placeholder="🔍 搜尋已存入之教材名稱或逐字稿關鍵字（如：民法、上課錄影、判例）..."
                          className="w-full bg-slate-950 border border-slate-850 focus:border-amber-500/80 rounded-lg py-2 pl-3 pr-8 text-xs text-slate-350 focus:outline-none transition-all font-sans"
                        />
                        {vaultSearchQuery && (
                          <button
                            type="button"
                            onClick={() => setVaultSearchQuery("")}
                            className="absolute right-2.5 top-2.5 text-slate-500 hover:text-slate-300 bg-transparent border-0 cursor-pointer"
                          >
                            <X className="w-3.5 h-3.5" />
                          </button>
                        )}
                      </div>
                    )}

                    <div className="space-y-3.5 max-h-[480px] overflow-y-auto pr-1">
                      {queueTab === "pending" ? (
                        batchTexts.filter(file => {
                          if (ingestFilter === "all") return true;
                          const fileCat = file.category || classifyFileCategory(file.name);
                          return fileCat === ingestFilter;
                        }).length === 0 ? (
                          <div className="text-center py-8 bg-slate-950/40 border border-slate-850 rounded-lg">
                            <p className="text-xs text-slate-500 italic font-sans">此分類篩選下無任何檔案項目。</p>
                          </div>
                        ) : (
                          batchTexts.filter(file => {
                            if (ingestFilter === "all") return true;
                            const fileCat = file.category || classifyFileCategory(file.name);
                            return fileCat === ingestFilter;
                          }).map((file, idx) => {
                            const origIdx = batchTexts.findIndex(b => b.name === file.name);
                            const activeCat = file.category || classifyFileCategory(file.name);
                            
                            if (file.status === "error") {
                              return (
                                <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-rose-950/40 rounded-lg flex flex-col gap-2.5 text-xs animate-[fadeIn_0.2s_ease-out]">
                                  <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                    <span className="text-rose-400 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                    <span className="text-slate-500 shrink-0">
                                      {file.size ? `${(file.size / (1024 * 1024)).toFixed(2)} MB` : "失敗"} | UTF-8
                                    </span>
                                  </div>
                                  <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/40 rounded border border-slate-850/30">
                                    <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                      <span>🏫 預定分類:</span>
                                      <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                        activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                        activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                        activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                        activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                        'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                      }`}>
                                        {activeCat}
                                      </span>
                                    </span>
                                  </div>
                                  <div className="p-2 border border-rose-500/20 bg-rose-550/5 rounded-lg text-rose-400 text-[11px] font-sans flex items-start gap-2">
                                    <AlertTriangle className="w-4 h-4 text-rose-400 shrink-0 mt-0.5" />
                                    <div className="flex-1 flex flex-col gap-1">
                                      <span className="font-bold">❌ 解析失敗：</span>
                                      <span className="font-mono text-[10px] text-rose-300 leading-normal break-all">{file.error || "未知錯誤"}</span>
                                    </div>
                                  </div>
                                </div>
                              );
                            }

                            return (
                              <div key={file.name + '-' + idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs animate-[fadeIn_0.2s_ease-out]">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-500 font-semibold truncate max-w-[280px]" title={file.name}>{file.name}</span>
                                  <span className="text-slate-500 shrink-0">{(file.content.length * 2 / 1024).toFixed(1)} KB | UTF-8</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 自動學術分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">分類變更:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        if (origIdx !== -1) {
                                          const newArr = [...batchTexts];
                                          newArr[origIdx] = { ...newArr[origIdx], category: e.target.value as any };
                                          setBatchTexts(newArr);
                                        }
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(file.content);
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            if (origIdx !== -1) {
                                              const newArr = [...batchTexts];
                                              newArr[origIdx].content = autoCorrectText(file.content);
                                              setBatchTexts(newArr);
                                            }
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5 font-sans">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-455 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-450 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                      <p className="text-[9px] text-slate-500 italic leading-none font-sans mt-0.5">
                                        提示：將滑鼠游標停留在出錯點上方，即可參閱 LanguageTool 智慧法學解析依據。
                                      </p>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={file.content}
                                  onChange={(e) => {
                                    if (origIdx !== -1) {
                                      const newArr = [...batchTexts];
                                      newArr[origIdx].content = e.target.value;
                                      setBatchTexts(newArr);
                                    }
                                  }}
                                  rows={3}
                                  className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-sans"
                                />
                              </div>
                            );
                          })
                        )
                      ) : (
                        (() => {
                          const filteredVaultItems = vaultItems.filter(item => {
                            // Category filter
                            if (ingestFilter !== "all") {
                              const fileCat = item.category || "學術教材";
                              if (fileCat !== ingestFilter) return false;
                            }
                            // Search filter
                            const q = vaultSearchQuery.toLowerCase().trim();
                            if (q) {
                              const matchesSource = item.source && item.source.toLowerCase().includes(q);
                              const matchesText = item.text && item.text.toLowerCase().includes(q);
                              const matchesCat = item.category && item.category.toLowerCase().includes(q);
                              if (!matchesSource && !matchesText && !matchesCat) return false;
                            }
                            return true;
                          });

                          if (filteredVaultItems.length === 0) {
                            return (
                              <div className="text-center py-8 bg-slate-950/40 border border-slate-850 border-dashed rounded-lg">
                                <p className="text-xs text-slate-500 italic font-sans">此篩選條件下智商庫無任何教材項目。</p>
                              </div>
                            );
                          }

                          return filteredVaultItems.map((item, idx) => {
                            const activeCat = item.category || "學術教材";
                            const fileExt = item.source.split('.').pop()?.toUpperCase() || "TXT";
                            
                            return (
                              <div key={item.id || idx} className="p-3 bg-slate-950 border border-slate-800 rounded-lg flex flex-col gap-2.5 text-xs animate-[fadeIn_0.2s_ease-out]">
                                <div className="flex justify-between items-center border-b border-slate-850 pb-1.5 mb-0.5 text-[11px] font-mono">
                                  <span className="text-amber-500 font-semibold truncate max-w-[250px]" title={item.source}>📄 {item.source}</span>
                                  <span className="text-slate-500 shrink-0">{(item.text ? item.text.length : 0)} 字 | {fileExt}</span>
                                </div>

                                {/* Classification Field */}
                                <div className="flex flex-wrap justify-between items-center gap-2 text-[10px] px-2 py-1.5 bg-slate-900/60 rounded border border-slate-800/40">
                                  <span className="text-slate-400 font-medium flex items-center gap-1.5">
                                    <span>🏫 目前智商分類:</span>
                                    <span className={`px-2 py-0.5 rounded text-[9px] font-bold border ${
                                      activeCat === '判例' ? 'bg-cyan-500/10 text-cyan-400 border-cyan-500/20' :
                                      activeCat === '證物' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' :
                                      activeCat === '學術錄影' ? 'bg-rose-500/10 text-rose-400 border-rose-500/20' :
                                      activeCat === '學術錄音' ? 'bg-purple-500/10 text-purple-450 border-purple-500/20' :
                                      'bg-indigo-500/10 text-indigo-400 border-indigo-500/20'
                                    }`}>
                                      {activeCat}
                                    </span>
                                  </span>
                                  <div className="flex items-center gap-1.5">
                                    <span className="text-slate-500 font-sans">分類變更:</span>
                                    <select
                                      value={activeCat}
                                      onChange={(e) => {
                                        const updated = vaultItems.map(vi => vi.id === item.id ? { ...vi, category: e.target.value } : vi);
                                        setVaultItems(updated);
                                      }}
                                      className="bg-slate-950 border border-slate-800 text-slate-300 rounded px-1.5 py-0.5 text-[10px] focus:outline-none focus:border-amber-500/80 cursor-pointer"
                                    >
                                      <option value="判例">⚖️ 判例</option>
                                      <option value="證物">📁 證物</option>
                                      <option value="學術錄影">🎥 學術錄影</option>
                                      <option value="學術錄音">📻 學術錄音</option>
                                      <option value="學術教材">📖 學術教材</option>
                                    </select>
                                  </div>
                                </div>

                                {/* Real-time spelling & Taiwanese legal terminology proofreading */}
                                {(() => {
                                  const detected = proofreadText(item.text || "");
                                  if (detected.length === 0) return null;
                                  return (
                                    <div className="bg-amber-500/5 border border-amber-500/20 p-2.5 rounded-lg flex flex-col gap-1.5 animate-[fadeIn_0.2s_ease-out]">
                                      <div className="flex items-center justify-between text-amber-400 font-bold text-[10px] font-sans">
                                        <span className="flex items-center gap-1">
                                          <AlertTriangle className="w-3.5 h-3.5 text-amber-500" />
                                          ⚠️ 偵測到 {detected.length} 處疑似語音或法律術語標記錯誤
                                        </span>
                                        <button
                                          type="button"
                                          onClick={() => {
                                            const updated = vaultItems.map(vi => vi.id === item.id ? { ...vi, text: autoCorrectText(vi.text) } : vi);
                                            setVaultItems(updated);
                                          }}
                                          className="px-2 py-0.5 bg-amber-500/20 hover:bg-amber-500 text-amber-300 hover:text-slate-950 rounded font-bold text-[10px] transition-all flex items-center gap-1 border border-amber-500/30 cursor-pointer active:scale-95"
                                        >
                                          <span>⚡ 一鍵本卡校正</span>
                                        </button>
                                      </div>
                                      <div className="flex flex-wrap gap-1 max-h-[85px] overflow-y-auto pr-1 pad-y-0.5 font-sans">
                                        {detected.map((match, mIdx) => (
                                          <span 
                                            key={match.id + '-' + mIdx} 
                                            className="inline-flex items-center bg-slate-950 border border-slate-850 rounded px-1.5 py-0.5 text-[10px] text-slate-300 font-mono select-none"
                                            title={`${match.reason} (建議修正為：${match.correct})`}
                                          >
                                            <span className="text-rose-455 line-through mr-1">{match.suspect}</span>
                                            <span className="text-slate-600">➔</span>
                                            <span className="text-emerald-450 font-bold ml-1">{match.correct}</span>
                                          </span>
                                        ))}
                                      </div>
                                    </div>
                                  );
                                })()}

                                <textarea 
                                  value={item.text || ""}
                                  onChange={(e) => {
                                    const updated = vaultItems.map(vi => vi.id === item.id ? { ...vi, text: e.target.value } : vi);
                                    setVaultItems(updated);
                                  }}
                                  rows={4}
                                  className="w-full bg-slate-900 border border-slate-850 text-slate-400 focus:outline-none focus:text-slate-100 rounded p-1.5 leading-normal mt-0.5 font-sans"
                                />

                                {/* Save, Copy, Delete Inline Buttons */}
                                <div className="flex justify-between items-center gap-2 mt-1 pt-1.5 border-t border-slate-900/60">
                                  <button
                                    type="button"
                                    onClick={() => deleteVaultItem(item.id)}
                                    className="px-2 py-1 bg-rose-500/10 hover:bg-rose-500 hover:text-white border border-rose-500/20 text-rose-400 rounded text-[10px] transition-all cursor-pointer flex items-center gap-1 active:scale-95 border-none"
                                  >
                                    <Trash2 className="w-3.5 h-3.5" />
                                    <span>🗑️ 永久刪除</span>
                                  </button>
                                  <div className="flex gap-2">
                                    <button
                                      type="button"
                                      onClick={() => {
                                        navigator.clipboard.writeText(item.text || "");
                                        showAlert("📋 全文已成功複製到剪貼簿！");
                                      }}
                                      className="px-2 py-1 bg-slate-900 hover:bg-slate-800 border border-slate-800 text-slate-355 rounded text-[10px] transition-all cursor-pointer flex items-center gap-1 active:scale-95"
                                    >
                                      <span>複製全文</span>
                                    </button>
                                    <button
                                      type="button"
                                      onClick={() => updateVaultItem(item.id, item.text, item.category)}
                                      className="px-2.5 py-1 bg-emerald-600 hover:bg-emerald-500 text-white rounded text-[10px] font-bold transition-all cursor-pointer flex items-center gap-1 active:scale-95 border-none"
                                    >
                                      <span>💾 儲存修改</span>
                                    </button>
                                  </div>
                                </div>
                              </div>
                            );
                          });
                        })()
                      )}
                    </div>

                    {queueTab === "pending" && (
                      <button 
                        onClick={handleBatchIngest}
                        disabled={isIngesting}
                        className="w-full py-3 bg-gradient-to-r from-amber-600 to-amber-700 text-white rounded-lg font-semibold hover:brightness-110 active:brightness-95 transition-all cursor-pointer text-xs flex items-center justify-center gap-2 mt-2 border-none"
                      >
                        {isIngesting ? <RefreshCw className="w-4 h-4 animate-spin text-white" /> : "🚀 確認校對無誤，開始智慧消化並寫入資料庫/智商庫"}
                      </button>
                    )}
                  </div>
                </div>

--------------------------------------------------------------------------------

=== Line 1455 | Step 1284 | Tool replace_file_content ===
Description: Removed the redundant bottom vault explorer block from App.tsx since it is now integrated into the right-hand queue panel.
Instruction: Delete the redundant bottom vault explorer section.
TargetContent:
              {/* PERSISTENT DATABASE / INTELLIGENCE VAULT EXPLORER */}
              <div className="mt-8 pt-6 border-t border-slate-800 flex flex-col gap-4">
                <div className="flex justify-between items-center">
                  <div>
                    <h3 className="text-sm font-bold text-slate-100 flex items-center gap-2">
                      <span>📚</span> 已成功導入資料庫/智商庫之教材逐字稿列表 ({vaultItems.length} 個項目)
                    </h3>
                    <p className="text-[11px] text-slate-500 font-sans mt-0.5">
                      列出所有已持久化寫入資料庫/智商庫之教材，您可在此進行關鍵字快速搜尋、查看逐字稿全文與 AI 消化後的法理爭點，或進行編輯、刪除管理。
                    </p>
                  </div>
                  <button
                    type="button"
                    onClick={fetchVaultItems}
                    disabled={isVaultLoading}
                    className="px-2.5 py-1 bg-slate-900 hover:bg-slate-850 border border-slate-800 text-slate-355 rounded text-xs cursor-pointer transition-colors flex items-center gap-1.5"
                  >
                    <RefreshCw className={`w-3.5 h-3.5 ${isVaultLoading ? "animate-spin" : ""}`} />
                    <span>整理清單</span>
                  </button>
                </div>

                {/* Filter and Search Bar */}
                <div className="flex flex-col sm:flex-row gap-3 bg-slate-900/40 p-3 border border-slate-850 rounded-xl">
                  <div className="flex-1 relative">
                    <input
                      type="text"
                      value={vaultSearchQuery}
                      onChange={(e) => setVaultSearchQuery(e.target.value)}
                      placeholder="🔍 搜尋已存入之教材名稱或逐字稿關鍵字（如：民法、上課錄影、判例）..."
                      className="w-full bg-slate-950 border border-slate-855 focus:border-amber-500/80 rounded-lg py-2 pl-3 pr-8 text-xs text-slate-350 focus:outline-none transition-all font-sans"
                    />
                    {vaultSearchQuery && (
                      <button
                        type="button"
                        onClick={() => setVaultSearchQuery("")}
                        className="absolute right-2.5 top-2.5 text-slate-500 hover:text-slate-300 bg-transparent border-0 cursor-pointer"
                      >
                        <X className="w-3.5 h-3.5" />
                      </button>
                    )}
                  </div>
                </div>

                {/* Ingested Items Grid List */}
                {isVaultLoading ? (
                  <div className="py-8 text-center bg-slate-950/40 border border-slate-855 rounded-lg flex flex-col items-center justify-center gap-2">
                    <RefreshCw className="w-6 h-6 animate-spin text-amber-500" />
                    <p className="text-xs text-slate-500">正在加載智商庫教材清單中...</p>
                  </div>
                ) : (() => {
                  const filtered = vaultItems.filter(item => {
                    const q = vaultSearchQuery.toLowerCase().trim();
                    if (!q) return true;
                    return (
                      (item.source && item.source.toLowerCase().includes(q)) ||
                      (item.text && item.text.toLowerCase().includes(q)) ||
                      (item.category && item.category.toLowerCase().includes(q))
                    );
                  });

                  if (filtered.length === 0) {
                    return (
                      <div className="py-8 text-center bg-slate-950/40 border border-slate-850 border-dashed rounded-lg">
                        <p className="text-xs text-slate-550 italic">無符合關鍵字之已導入教材，或您尚未載入教材入庫。</p>
                      </div>
                    );
                  }

                  return (
                    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3.5 max-h-[380px] overflow-y-auto pr-1">
                      {filtered.map((item, vIdx) => {
                        const fileExt = item.source.split('.').pop()?.toUpperCase() || "TXT";
                        const cleanSnippet = item.text
                          ? item.text.replace(/【多模態餵養導入：[^】]+】\n?/, "").slice(0, 160) + "..."
                          : "無內容";
                        return (
                          <div 
                            key={item.id || vIdx} 
                            onClick={() => {
                              setSelectedVaultItem(item);
                              setVaultEditText(item.text || "");
                              setIsVaultEditMode(false);
                            }}
                            className="p-3 bg-slate-900 border border-slate-850 hover:border-amber-500/50 hover:bg-slate-900/80 rounded-xl transition-all flex flex-col gap-2.5 text-xs cursor-pointer group shadow-sm hover:shadow-md relative overflow-hidden"
                          >
                            <div className="absolute top-0 left-0 w-full h-[2.5px] bg-gradient-to-r from-amber-600 to-amber-700 opacity-60 group-hover:opacity-100 transition-opacity" />
                            <div className="flex justify-between items-start gap-1.5">
                              <span className="font-mono font-bold text-slate-200 group-hover:text-amber-500 transition-colors truncate flex-1" title={item.source}>
                                📄 {item.source}
                              </span>
                              <span className="px-1.5 py-0.5 bg-slate-950 border border-slate-850 text-[8px] font-bold text-slate-400 rounded shrink-0">
                                {fileExt}
                              </span>
                            </div>
                            <p className="text-[11px] text-slate-400 line-clamp-3 leading-relaxed font-sans mt-0.5">
                              {cleanSnippet}
                            </p>
                            <div className="mt-1 flex justify-between items-center text-[10px] border-t border-slate-855/60 pt-2 text-slate-500">
                              <span>字數: {item.text ? item.text.length : 0} 字</span>
                              <span className="text-amber-500 hover:text-amber-400 font-bold flex items-center gap-0.5">
                                🔍 點擊查看/校對 ➔
                              </span>
                            </div>
                          </div>
                        );
                      })}
                    </div>
                  );
                })()}
              </div>

ReplacementContent:


--------------------------------------------------------------------------------

