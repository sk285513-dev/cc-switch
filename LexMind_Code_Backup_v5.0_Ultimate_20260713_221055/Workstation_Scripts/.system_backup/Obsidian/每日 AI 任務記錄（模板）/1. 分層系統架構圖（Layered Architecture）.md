graph TD
    %% 層級定義
    subgraph L7[基礎設施層 Infrastructure Layer]
        OS[Windows 11 + WSL2]
        Docker[Docker Desktop<br/>RAGFlow Container]
        GPU[RTX 5060 Ti 16GB]
        PyEnv[Python 3.12 + venv]
    end

    subgraph L6[模型與推理層 Model & Reasoning Layer]
        Ollama[Ollama Service]
        Gemma[Gemma2:9b<br/>Reasoning / Judge]
        DSCoder[DeepSeek-Coder<br/>Coding Worker A]
        QwenCoder[Qwen2.5-Coder:7B<br/>Coding Worker B]
        Embed[bge-m3 或其他 Embedding 模型]
    end

    subgraph L5[知識與檢索層 Knowledge & Retrieval Layer]
        subgraph ObVault[Obsidian Vault]
            Ob00[00_系統架構]
            Ob01[01_研究筆記]
            Ob02[02_程式碼與自動化]
            Ob03[03_法律與案件]
            Ob04[04_任務日誌]
            Ob05[05_知識索引 (MOC)]
        end

        subgraph RAGFlow[RAGFlow Server]
            subgraph Datasets
                DS_arch[research_architecture]
                DS_rag[research_rag]
                DS_agents[research_agents]
                DS_models[research_models]
                DS_py[code_python]
                DS_ps[code_powershell]
                DS_codedoc[code_docs]
                DS_trbl[troubleshooting_cases]
                DS_lstat[legal_statutes]
                DS_lcase[legal_cases]
                DS_lnotes[legal_case_notes]
                DS_daily[daily_work_logs]
            end
            Ingest[Ingestion Pipeline<br/>Parsing / Chunking]
            Retr[Retrieval Engine<br/>Hybrid + Rerank]
        end
    end

    subgraph L4[設定與同步層 Config & Sync Layer]
        ConfigSup[PowerShell Config Supervisor<br/>init_or_validate_config.ps1]
        SyncEng[Python Sync Engine<br/>sync_obsidian_to_ragflow.py]
        CfgFile[config.json]
        SyncIdx[sync_index.json]
    end

    subgraph L3[互動層 Interaction Layer]
        ObsidianApp[Obsidian App]
        RAGUI[RAGFlow Web UI]
        FutureUI[未來：Open WebUI / OpenHands / 自訂 Agent UI]
    end

    subgraph L2[使用者層 User Layer]
        User[使用者<br/>研究 / 開發 / 法律分析]
    end

    %% 連線：基礎 → 模型
    OS --> Docker
    OS --> PyEnv
    OS --> Ollama
    GPU --> Docker
    GPU --> Ollama

    %% 模型層內部
    Ollama --> Gemma
    Ollama --> DSCoder
    Ollama --> QwenCoder
    Ollama --> Embed

    %% RAGFlow ↔ 模型
    Docker --> RAGFlow
    RAGFlow -. HTTP API .- Ollama

    %% 設定與同步
    ConfigSup --> CfgFile
    ConfigSup --> SyncEng
    SyncEng --> CfgFile
    SyncEng --> SyncIdx
    SyncEng --> ObVault
    SyncEng --> RAGFlow

    %% Obsidian Vault 結構
    Ob00 --> Ob01
    Ob00 --> Ob02
    Ob00 --> Ob03
    Ob00 --> Ob04
    Ob00 --> Ob05

    %% Datasets 與 Ingestion / Retrieval
    Ingest --> Datasets
    Retr --> Datasets

    %% Interaction
    User --> ObsidianApp
    User --> RAGUI
    User --> FutureUI

    ObsidianApp --> ObVault
    RAGUI --> RAGFlow
    FutureUI --> RAGFlow

    %% 檢索與推理路徑
    RAGFlow --> Retr
    Retr --> Gemma
    Retr --> DSCoder
    Retr --> QwenCoder

    %% 標註關鍵關係
    classDef highlight fill=#fffbcc,stroke=#e0c200,stroke-width=1px;
    ConfigSup:::highlight
    SyncEng:::highlight
    RAGFlow:::highlight
    Ollama:::highlight